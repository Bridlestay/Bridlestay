# Changelog - 2026-01-30

## Major Routes System Redesign - OS Maps Style

Today marks a major overhaul of the routes system to create an experience similar to OS Maps. The goal is to have a map-first, app-like interface where the map takes center stage.

---

## 1. New Map-First Layout

**What Changed:**
The routes page now features a full-screen map (100% viewport height) instead of the previous card-based layout with a map in a small container.

**Before:** 
- Map in a `h-96` card (fixed 384px height)
- Route list below/beside the map
- Traditional web page layout with header and container padding

**After:**
- Map fills the entire screen (`fixed inset-0`)
- Overlay panels float on top of the map
- App-like experience similar to OS Maps

**Files Modified:**
- `app/routes/page.tsx` - Complete rewrite with new layout

---

## 2. Custom Map Controls (OS Maps Style)

**What Changed:**
Removed all default Google Maps UI controls and replaced with custom controls positioned bottom-right:

**New Controls:**
1. **Layer Toggle Button** - Opens the layer settings panel
2. **GPS Location Button** - Centers map on user's location
3. **Fullscreen Button** - Toggles fullscreen mode
4. **Zoom Controls** - Plus/minus buttons in a pill shape

**Removed:**
- Google Maps type selector
- Google Maps zoom controls
- Google Maps fullscreen button

**Files Created:**
- `components/routes/map-layer-controls.tsx` - New custom controls component

**Files Modified:**
- `components/routes/routes-map-v2.tsx` - Disabled Google's default UI

---

## 3. Layer Settings Panel

**What Changed:**
Created a slide-up panel (mobile) / side panel (desktop) for map layer settings.

**Features:**
- **Map Type Selection** - Visual buttons for Standard, Topographic, Aerial
- **Path Layer Toggles:**
  - Bridleways (brown) - ON by default
  - Byways/BOATS (purple) - ON by default
  - Restricted Byways (orange) - ON by default
  - Footpaths (green) - OFF by default (hidden to discourage hikers)
- **Feature Toggles:**
  - Waymarkers visibility
  - Hazards visibility
  - Properties visibility
- **Route Line Width** - Slider to adjust route line thickness (2-8px)

**Files Created:**
- `components/routes/map-layer-controls.tsx`

---

## 4. Search Panel (OS Maps Style)

**What Changed:**
Created a collapsible search panel that slides in from the left side.

**Features:**
- **Toggle Button** - Hamburger menu when closed, chevron when open
- **Search Bar** - Search for places and routes
- **Tabs** - Places / Routes (like OS Maps)
- **Filters Panel:**
  - Distance range slider (0-50+ miles)
  - Difficulty buttons (Unrated, Easy, Moderate, Difficult, Severe)
  - Minimum rating filter (Any, 3+, 3.5+, 4+, 4.5+)
  - Route type filter (Circular, Point-to-point)
- **Routes List** - Scrollable list with cover photos, stats, badges
- **Create Route Button** - At the bottom of the panel

**Files Created:**
- `components/routes/map-search-panel.tsx`

---

## 5. Route Bottom Sheet

**What Changed:**
When clicking a route on the map, instead of opening a drawer immediately, a bottom sheet appears with route preview info.

**Features:**
- **Single Route View:**
  - Cover image, title, creator info
  - Distance, duration, rating
  - Difficulty and route type badges
  - Close button
  - Tap to open full details
  
- **Cluster/Multiple Routes View (Carousel):**
  - Horizontal scrollable carousel of route cards
  - Each card shows thumbnail, title, stats
  - Tap to highlight route on map
  - Double-tap to open full details
  - Navigation arrows on desktop

**Files Created:**
- `components/routes/route-bottom-sheet.tsx`

---

## 6. Footpaths Hidden by Default

**What Changed:**
As requested, footpaths are now hidden by default to discourage walkers/hikers and reduce API usage.

**Implementation:**
- `showFootpaths` defaults to `false` in LayerSettings
- Toggle is still available in the layer panel but styled with reduced opacity
- Labeled as "(Hidden)" to indicate it's off by default

---

## 7. Route Creation Mode Updates

**What Changed:**
Route creation now also uses the full-screen map layout:
- Left sidebar with route creation form and path toggles
- Full-screen map for plotting
- Floating toolbar for plotting tools
- Custom map controls available during creation

---

## Files Summary

### New Files Created:
1. `components/routes/map-layer-controls.tsx` - Custom map controls (layers, GPS, fullscreen, zoom)
2. `components/routes/map-search-panel.tsx` - Collapsible search/filter panel
3. `components/routes/route-bottom-sheet.tsx` - Route info bottom sheet with carousel

### Files Modified:
1. `app/routes/page.tsx` - Complete rewrite with new map-first layout
2. `components/routes/routes-map-v2.tsx` - Disabled Google Maps default UI

---

## 8. Route Clustering (NEW)

**What Changed:**
Implemented marker clustering using `@googlemaps/markerclusterer` to group nearby route markers when zoomed out.

**Implementation:**
- Added `@googlemaps/markerclusterer` package
- Route start points are now marked with circular markers
- When routes are close together, they cluster into a single marker showing the count
- Clicking a cluster shows all routes in that area via the bottom sheet carousel
- Custom cluster styling with blue circles and white count labels

**Files Modified:**
- `components/routes/routes-map-v2.tsx` - Added MarkerClusterer integration
- `app/routes/page.tsx` - Added `onClusterClick` handler

---

## 9. GPS-Based Hazard Placement Restriction (NEW)

**What Changed:**
Hazard reports now require the user to be near the route. This prevents spam/false hazard reports from users who aren't actually on the route.

**Implementation:**
- When user opens the hazard dialog, their GPS location is checked
- User must be within **500 meters** of the route to submit a hazard
- Shows status banner:
  - "Checking your location..." (with spinner)
  - "Location verified - you're near the route" (green, can submit)
  - "Too far from route" (orange, shows distance, retry button)
  - "Location Error" (red, shows error message, retry button)
- **Admins and route owners** can bypass the location check
- Location is calculated using Haversine formula

**Files Modified:**
- `components/routes/route-detail-drawer.tsx` - Added location checking logic and UI

---

## 10. Collapsible Discussion Preview (NEW)

**What Changed:**
The Discussion (comments) tab now has a collapsed preview mode with horizontal scrolling.

**Implementation:**
- **Collapsed View (Default):**
  - Shows "X Comments" header
  - Horizontal scroll carousel of top 5 comment preview cards
  - Each card shows avatar, name, truncated comment (2 lines), timestamp, reply count
  - "View All X Comments" expand button
- **Expanded View:**
  - Full comment input form
  - Full Instagram-style comment thread with replies
  - "Collapse Discussion" button at top

**Why:**
- Reduces visual clutter in the route detail panel
- Allows quick preview without loading full discussion
- Better mobile experience with horizontal swiping

**Files Modified:**
- `components/routes/route-detail-drawer.tsx` - Added collapsed preview mode

---

## Remaining Tasks (for future updates)

The following features from the review are not yet implemented:

1. **Map Type Implementation** - Actually switch map tiles when map type is selected (currently just UI)
2. **Monochromatic Map Option** - Add grayscale map styling option
3. **Route Highlighting on Selection** - Highlight the selected route polyline when scrolling through carousel

---

## How to Test

1. Go to `/routes` - You should see a full-screen map
2. Click the hamburger menu (top-left) - Search panel slides in
3. Click the layers icon (bottom-right) - Layer settings panel appears
4. Click on any route on the map - Bottom sheet with route info appears
5. Click "Create Route" - Enters full-screen creation mode with sidebar

### New Features Testing:
6. **Route Clustering** - Zoom out on the map, nearby routes should cluster into numbered circles
7. **Cluster Click** - Click on a cluster circle, should show route carousel at bottom
8. **Hazard Reporting** - Open route details → Hazards tab → "Report a Hazard"
   - Should request GPS location
   - If near route: shows green "verified" message and form
   - If far: shows orange "too far" message with retry button
   - Admins/owners bypass location check
9. **Discussion Preview** - Open route details → Discussion tab
   - Should see horizontal scroll preview of comments
   - Click "View All X Comments" to expand full discussion
   - Click "Collapse Discussion" to return to preview

---

## 11. OS Maps-Style Navigation Tabs (NEW)

**What Changed:**
Added top-right navigation tabs like OS Maps: Map, Saved Routes, Find Routes, Create Route.

**Files Created:**
- `components/routes/routes-nav-tabs.tsx` - Navigation tab component

---

## 12. Saved Routes Panel (NEW)

**What Changed:**
Implemented a "Saved Routes" panel that shows user's own routes and bookmarked routes.

**Features:**
- Tabs for "My Routes" and "Bookmarked Routes"
- Sort by date, name, or distance
- Search functionality
- Route type filters (circular/linear)

**Files Created:**
- `components/routes/saved-routes-panel.tsx`
- `app/api/routes/my-routes/route.ts`
- `app/api/routes/favorites/route.ts`

---

## 13. Find Routes Panel (NEW)

**What Changed:**
Implemented a comprehensive "Find Routes" panel for route discovery.

**Features:**
- Distance range slider (0-40km)
- Difficulty filter buttons (Unrated, Easy, Moderate, Difficult, Severe)
- Rating filter (Any, 3.5+, 4.0+, 4.5+)
- Route type checkboxes
- "Routes Near Me" button
- Share view functionality

**Files Created:**
- `components/routes/find-routes-panel.tsx`

---

## 14. Route Highlighting on Selection (NEW)

**What Changed:**
Routes now highlight with a glow/thicker stroke when selected in the carousel or hovered in panels.

**Implementation:**
- Added `highlightedRouteId` prop to map component
- Route polylines thicken to 8px and increase zIndex when highlighted
- Works with both direct selection and hover events

---

## 15. Map Type Implementation (NEW)

**What Changed:**
Map type buttons now actually switch the map tiles.

**Implementation:**
- Standard → Google "roadmap"
- Topographic → Google "terrain"
- Aerial → Google "satellite"
- Added monochrome toggle that applies grayscale styling

---

## 16. Route Following/Navigation Mode (NEW)

**What Changed:**
Full turn-by-turn navigation for following routes on horseback.

**Features:**
- GPS tracking with position arrow marker
- Progress bar showing distance completed
- Audio announcements (text-to-speech)
- Hazard alerts when approaching hazards
- Waypoint notifications
- Off-route warnings (>50m from route)
- Expandable stats panel
- Settings for toggling alert types
- Pause/Resume/Reset/End controls

**Files Created:**
- `components/routes/route-navigator.tsx`

---

## 17. Route Recording (Strava-Style) (NEW)

**What Changed:**
Users can now record routes by riding them instead of manually plotting.

**Features:**
- GPS tracking with live stats
- Distance, time, speed (current/average/max)
- GPS accuracy indicator
- Recorded path drawn on map
- Pause/Resume functionality
- Save dialog with route details
- Discard confirmation

**Files Created:**
- `components/routes/route-recorder.tsx`

---

## 18. Post-Ride Review System (NEW)

**What Changed:**
After completing a route, users are prompted to rate and review it.

**Features:**
- 5-star rating system
- Difficulty rating (Easy/Moderate/Difficult/Severe)
- Optional written review
- Ride stats display
- Updates route's average rating and community difficulty

**Files Created:**
- `components/routes/post-ride-review.tsx`
- `app/api/routes/[routeId]/review/route.ts`

---

## 19. Elevation Profile (NEW)

**What Changed:**
Added interactive elevation chart for route visualization.

**Features:**
- Visual elevation graph
- Interactive hover to see elevation at any point
- Total ascent/descent statistics
- Distance markers

**Files Created:**
- `components/routes/elevation-profile.tsx`

---

## 20. Path Layers Auto-Toggle (NEW)

**What Changed:**
Path layers (bridleways, byways, etc.) are now automatically hidden in explore mode and enabled in create mode.

**Reason:**
- Reduces visual clutter when browsing
- Reduces API usage
- Enables path layers when needed for route creation/snapping

---

## 21. Monochrome Map Option (NEW)

**What Changed:**
Added grayscale map toggle that desaturates the map to make route lines stand out more.

---

---

## 22. Routes Page Fixes (Late Update)

**What Changed:**
Fixed several bugs and UX issues with the routes page:

### A. Route Record Button - Mobile Only
- The "Start Recording" button was showing on desktop where it doesn't work properly
- Now hidden on desktop (`md:hidden` class), only shows on mobile devices where GPS tracking works
- Recording is designed for mobile use when actually riding a route

### B. Home/Logo Navigation Added
- Added a padoq logo button in the top-left corner of the routes page
- Clicking the logo returns users to the home page (`/`)
- Users were previously stuck on the routes page with no way back

### C. Navigation Tab Order Fixed
- Previous order: Map, Saved Routes, Find Routes, Create Route
- New order: Map, Find Routes, Saved Routes, Create Route
- "Find Routes" is now to the left of "Saved Routes" as requested

### D. Create Route Toolbar Positioning Fixed
- The toolbar (Plot, Erase, Insert, Snap, etc.) was overlapping the navigation tabs
- Now positioned properly to the right of the sidebar (`left-[25rem]`)
- In create mode, the nav tabs are hidden (except the logo) to prevent overlap

### E. Routes Now Display on Map
- **Critical fix:** Routes were not appearing on the map because the API wasn't returning geometry data
- Added `geometry` and `route_type` fields to the route search API response
- Routes now display as thick, smooth, clickable polylines on the map
- Works for: Main map view, Find Routes panel, Saved Routes panel

**Files Modified:**
- `app/routes/page.tsx` - RouteRecorder mobile-only, hideInCreateMode prop
- `components/routes/routes-nav-tabs.tsx` - Added home logo, reordered tabs, hideInCreateMode prop
- `components/routes/route-creator.tsx` - Fixed toolbar positioning
- `app/api/routes/search/route.ts` - Added geometry and route_type to SELECT

---

*Changelog created: 2026-01-30*
*Last updated: 2026-01-30 (Routes page fixes - toolbar, logo, mobile recording, route display)*
*Author: AI Assistant (Claude)*

