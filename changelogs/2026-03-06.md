# Changelog - 2026-03-06

## Session 1: Waypoint Edit Fixes & Photo Integration

**Summary:** Fixed multiple bugs in the waypoint editing system — save URL, tag/icon persistence, photo FK, photo display in timeline, and tag label consistency.

---

### Changes Made

#### 1. Fix Waypoint Save URL (404)

**Modified Files:**
- `components/routes/edit-waypoint-view.tsx`

**What Changed:**
- Fixed PATCH URL from `/api/waypoints/${id}` (non-existent) to `/api/routes/${routeId}/waypoints/${id}` (actual endpoint)
- This was why saving waypoint edits silently failed with 404

#### 2. Fix PATCH Endpoint null Handling

**Modified Files:**
- `app/api/routes/[id]/waypoints/[waypointId]/route.ts`

**What Changed:**
- Fixed PATCH endpoint to accept `null` for `icon_type` and `tag` fields
- Used `!== undefined` instead of truthy check so clearing a field works

#### 3. Fix Waypoint Photo Upload FK Error

**Modified Files:**
- `app/api/routes/[id]/waypoints/[waypointId]/photos/route.ts`
- `supabase/migrations/075_fix_waypoint_photos_fk.sql` (NEW)

**What Changed:**
- `waypoint_photos.user_id` FK referenced `auth.users(id)` instead of `public.users(id)`
- PostgREST join failed with 500 errors because it couldn't traverse the FK
- Removed explicit FK hint from join queries
- Migration 075 drops and re-creates FK pointing to `public.users(id)`

**Risk:** Migration 075 must be run in production Supabase SQL Editor.

#### 4. Fix Tag/Icon Not Persisting

**Modified Files:**
- `components/routes/edit-waypoint-view.tsx`

**What Changed:**
- Fixed state initialization from `useState("")` + useEffect to `useState(waypoint.prop || "")`
- Sending `tag: tag || null` and `icon_type: iconType || null` instead of empty strings
- Empty string `""` violated CHECK constraint `tag IN ('poi', 'instruction', 'caution', 'note')`

#### 5. Fix Tag Label Mismatch + Missing "Note" Tag

**Modified Files:**
- `components/routes/waypoint-card.tsx`

**What Changed:**
- Added missing `note` entry to `TAG_CONFIG` (was only instruction/poi/caution)
- Waypoints with `tag: "note"` now display "Note" badge on the card
- Changed `poi` label from "POI" to "Point of Interest" for consistency with edit form
- Styled "Note" tag with slate colors

#### 6. Fix Waypoint Photos Not Showing on Cards

**Modified Files:**
- `app/api/routes/[id]/waypoints/route.ts`

**What Changed:**
- API returned `community_photos` property but `waypoint-card.tsx` reads from `photos`
- Added `photos` as an alias of `community_photos` in the API response
- Waypoint cards in the timeline now display photo thumbnails

#### 7. Owner Photo Upload Without Completion

**Modified Files:**
- `app/api/routes/[id]/waypoints/[waypointId]/photos/route.ts`

**What Changed:**
- Route owner can upload photos to their own waypoints without completing the route
- Previously ALL uploads required route completion, blocking owner from adding photos during editing

#### 8. Hide Edit on Start/Finish Waypoints

**Modified Files:**
- `components/routes/waypoint-timeline.tsx`

**What Changed:**
- Added `!isStart && !isFinish` conditions to `onEdit` and `onSuggestEdit` callbacks
- Start and Finish are virtual waypoints derived from geometry, not DB records

#### 9. View All Waypoint Map Toggle

**Modified Files:**
- `app/routes/page.tsx`

**What Changed:**
- `handleEnterViewMode("waypoints")` now sets `showWaymarkers: true` in layer settings
- Added exit effect: waypoints view mode auto-exits when `showWaymarkers` toggled off
- Matches existing hazards pattern

---

### SQL Migrations to Run
```sql
-- Migration 075: Run in Supabase SQL Editor
ALTER TABLE waypoint_photos
  DROP CONSTRAINT IF EXISTS waypoint_photos_user_id_fkey;

ALTER TABLE waypoint_photos
  ADD CONSTRAINT waypoint_photos_user_id_fkey
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL;
```

---

### Verification
- [x] `npm run build` passes
- [ ] Waypoint edits save successfully (name, tag, icon, description)
- [ ] Tag and icon persist after saving and re-opening edit view
- [ ] "Note" tag displays on waypoint cards
- [ ] "Point of Interest" label consistent between card and edit form
- [ ] Waypoint photos appear as thumbnails on timeline cards
- [ ] Photos can be uploaded in edit view (after running migration 075)
- [ ] Start/Finish waypoints do not show edit/suggest buttons
- [ ] "View all" toggles waypoint markers in map settings

---

## Session 2: Waypoint Card Komoot-Style Redesign

**Summary:** Rewrote the waypoint card component to match Komoot's clean, always-visible layout with prominent photo collages instead of the old expand/collapse pattern with tiny thumbnails.

---

### Changes Made

#### 1. Redesign Waypoint Card Layout

**Modified Files:**
- `components/routes/waypoint-card.tsx`

**What Changed:**
- Removed expand/collapse toggle — all content now always visible (like Komoot)
- Removed `Card`, `Badge`, `ChevronDown`, `ChevronUp` imports (no longer needed)
- Added `Image as LucideImage` import for photo count badge icon

**New layout (top to bottom):**
- **Photo collage** (if photos exist):
  - 1 photo: full-width `h-36` rounded image
  - 2 photos: side-by-side 50/50 split, `h-32`
  - 3+ photos: Komoot collage — large image (2/3 width) + 2 stacked images (1/3 width), `h-40`, with "N images" badge
- **Name**: bold text, always visible
- **Icon + Tag inline**: Komoot-style `<Icon> Icon Label · Tag Label` with dot separator
- **Description**: `line-clamp-2` with "Read more" button for long text
- **Actions**: "Show on map" + edit pencil, always visible

**Removed:**
- `expanded` state and chevron toggle
- Separate collapsed view (tiny thumbnail + name) and expanded view (badges + photo grid)
- `Badge` component usage for icon/tag display

---

### Verification
- [x] `npm run build` passes
- [ ] Waypoints with 1 photo show full-width image
- [ ] Waypoints with 2 photos show side-by-side layout
- [ ] Waypoints with 3+ photos show Komoot-style collage with image count badge
- [ ] Waypoints without photos show clean text-only card
- [ ] Icon and tag display inline with dot separator
- [ ] Long descriptions truncate with "Read more"
- [ ] Start/Finish waypoints render cleanly (just name)

---

## Session 3: Suggestion Review UI + Pending Count Badge

**Summary:** Added a suggestion review panel inside the edit waypoint view so route owners can see, approve, or reject community-suggested edits. Also wired up the pending suggestion count badge on waypoint cards.

---

### Changes Made

#### 1. Add Pending Suggestion Counts to Waypoints API

**Modified Files:**
- `app/api/routes/[id]/waypoints/route.ts`

**What Changed:**
- When the authenticated user is the route owner, the API now batch-queries `waypoint_edit_suggestions` for pending counts
- Each waypoint in the response includes `pending_suggestions_count` (only for owners)
- This powers the amber badge on the pencil icon in waypoint cards
- Uses a single batch query (all waypoint IDs at once) to avoid N+1

#### 2. Suggestion Review UI in Edit Waypoint View

**Modified Files:**
- `components/routes/edit-waypoint-view.tsx`

**What Changed:**
- Fetches pending edit suggestions on mount via `GET /api/waypoints/{id}/edit-suggestions`
- Displays an amber-bordered section between the title and form fields when suggestions exist
- Each suggestion card shows:
  - User avatar, username, and relative timestamp
  - Quoted suggestion comment in italics
  - Diff view: each changed field shows `Current → Suggested` with arrow
  - Human-readable labels for tags and icons (not raw values)
  - "Apply changes" (green) and "Reject" (outline) buttons
- Approving a suggestion: calls PATCH API, updates form fields to match, removes from list
- Rejecting: shows inline textarea for reason, then calls PATCH API
- Loading states and spinners on buttons during processing
- New `SuggestionDiff` helper component for clean diff display
- Added `Button`, `Avatar`, `formatDistanceToNow` imports

---

### Verification
- [x] `npm run build` passes
- [ ] Owner sees amber badge count on waypoint pencil icon
- [ ] Opening edit view shows pending suggestion cards
- [ ] Suggestion cards display user info, comment, and field diffs
- [ ] "Apply changes" approves and updates form fields
- [ ] "Reject" shows reason input, submits rejection
- [ ] After approve/reject, suggestion disappears from list
- [ ] Non-owners don't see suggestion counts (not exposed in API)

---

## Session 4: Photo Suggestions, Auth Guard, and UI Polish

**Summary:** Added photo upload support to the edit suggestion system so users can suggest new photos for waypoints. Owners can accept just photos without applying text changes. Also added auth guard on /routes page and shrank the suggest edit button to icon-only.

---

### Changes Made

#### 1. Shrink Suggest Edit Button to Icon-Only

**Modified Files:**
- `components/routes/waypoint-card.tsx`

**What Changed:**
- Changed "Suggest an edit" from full `<Button variant="outline">` to a plain icon button
- Now matches the owner's edit pencil style — just a small pencil icon
- Added `title="Suggest an edit"` for hover tooltip
- Cleaner visual weight on waypoint cards

#### 2. Auth Guard on /routes Page

**Modified Files:**
- `app/routes/page.tsx`

**What Changed:**
- Added auth check on mount — redirects unauthenticated users to `/auth/sign-in`
- Shows green spinner while auth state is being checked
- Uses `router.replace()` so users can't navigate back to the routes page

#### 3. Photo Suggestions in Suggest Edit Dialog

**Modified Files:**
- `components/routes/suggest-edit-waypoint-dialog.tsx`

**What Changed:**
- Added photo upload section with dashed upload area and image previews
- Photos upload to storage via `?suggestion=true` param (skips DB insert)
- Users can remove uploaded photos before submitting
- Suggested photos included in submission payload as `suggested_photos` JSONB
- Fixed tag default from hardcoded `"note"` to `waypoint.tag || ""`
- Added `routeId` prop for photo upload API calls
- Added `ImagePlus`, `X`, `Loader2`, and `Image` imports

#### 4. Photo Suggestion API Support

**Modified Files:**
- `app/api/routes/[id]/waypoints/[waypointId]/photos/route.ts`
- `app/api/waypoints/[id]/edit-suggestions/route.ts`
- `app/api/waypoints/[id]/edit-suggestions/[suggestionId]/route.ts`

**What Changed:**
- **Photos API**: Added `?suggestion=true` mode — uploads to `suggestions/` subdirectory, returns URL without creating DB record, skips route completion check
- **POST edit-suggestions**: Now accepts `suggested_photos` array; validates that at least one text change OR photo change exists
- **PATCH edit-suggestions**: Added `approve_photos` action — creates `waypoint_photos` DB records from suggested URLs without applying text changes. Uses service client to bypass RLS.

#### 5. Photo Review UI for Owners

**Modified Files:**
- `components/routes/edit-waypoint-view.tsx`
- `components/routes/route-detail-drawer.tsx`

**What Changed:**
- Suggestion review cards now show photo thumbnails in a 3-column grid
- Added "Accept photos only" button (green outline with ImagePlus icon) — shown when suggestion has both text and photo changes
- Renamed "Apply changes" to "Apply all changes" for clarity
- Added `SuggestedPhotoEntry` interface and `suggested_photos` to `EditSuggestion` type
- Drawer now passes `routeId` prop to `SuggestEditWaypointDialog`

---

### Verification
- [x] `npm run build` passes
- [ ] Suggest edit button is icon-only with hover tooltip
- [ ] Unauthenticated users redirected from /routes to /auth/sign-in
- [ ] Suggest edit dialog shows photo upload area
- [ ] Photos upload successfully with `?suggestion=true`
- [ ] Photo previews show in dialog with remove button
- [ ] Photo-only suggestions can be submitted (no text changes required)
- [ ] Owner sees suggested photo thumbnails in review panel
- [ ] "Accept photos only" button appears for mixed text+photo suggestions
- [ ] "Accept photos only" creates waypoint_photos without applying text changes
- [ ] "Apply all changes" applies both text and photos
