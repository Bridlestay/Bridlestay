# Changelog — 2026-02-24

## Session: UX Polish, Hazard Fixes, UI Design Guide

Continued from the 2026-02-22 session (community warnings + waypoint tags). This session focused on UX issues found during testing and establishing a formal UI/UX design guide.

---

### Bug Fixes

#### 1. Clickable Hazards Badge
The red hazards count badge at the top of the route card was not clickable (unlike the warnings badge). Now clicking it opens the hazards map view mode, or scrolls to the hazards section if the map view isn't available.

#### 2. Hazard Popup Persistence
Hazard map popups were dismissed instantly on mouse leave, making it impossible to click the "Mark as Cleared" button. Added a 200ms close delay with hover detection on the popup itself — users can now move their mouse from the marker to the popup to interact with it.

#### 3. Hazards Default Off in Map Settings
`showHazards` was defaulting to `true` in layer settings. Changed to `false` so hazard markers don't show until explicitly toggled on.

#### 4. Layer Settings Toggle Works for Hazards
Hazard markers were only shown in view mode (`mapViewMode === "hazards"`) and ignored the layer settings toggle. Now they respond to both, matching the existing pattern for waypoints: `mapViewMode === "hazards" || layerSettings.showHazards`.

#### 5. Back Button Visible on Mobile
The "Back to route details" button in view mode (waypoints/hazards) had `z-30` but the mobile header has `z-50`, making it hidden. Raised to `z-[60]` and repositioned to `top-14` so it sits below the mobile header and is always clickable.

#### 6. Hazards Toggle Syncs with View Mode
Clicking the hazards badge on the route card enters hazards view mode (`mapViewMode = "hazards"`), which shows hazard markers on the map. However, the hazards toggle in map settings didn't reflect this — it stayed off. Now `handleEnterViewMode("hazards")` also sets `layerSettings.showHazards = true`, so the toggle is in sync.

#### 7. Back Button No Longer Overlaps Header
The "Back to route details" button in hazards/waypoints view mode was overlapping the header on both mobile and desktop. Unified position to `top-16` (64px) on all breakpoints so it sits below the mobile header and below the desktop map header (hamburger/search/POI bar). Removed redundant `md:top-4` override.

#### 8. Toggling Hazards Off in Settings Exits View Mode
When the user entered hazards view mode via the badge and then toggled hazards off in map settings, the markers remained because `mapViewMode === "hazards"` was still true. Added a `useEffect` that exits hazards view mode when `layerSettings.showHazards` is set to false.

#### 9. Removed "Back to Route Details" Button
The floating "Back to route details" button in waypoints/hazards view mode was removed. Clicking the route pin to re-open the route card is the natural, obvious navigation pattern and the extra button was cluttering the map UI.

#### 10. Route Pins Redesigned (OS Maps Style)
Route map pins updated to an OS Maps–inspired style: thick white outline (3px stroke), green-600 (`#16A34A`) fill, white center dot. Previously used a solid dark green (`#166534`) pin without a visible border. The new pins are slightly larger (32x42 vs 28x36) for better visibility and feel more polished.

### New File: UI/UX Design Guide

Created a comprehensive UI/UX design ruleset at `Agent Guide (Please Read)/UI Design Guide/UI_UX_DESIGN_RULES.md` based on the 10 core UX principles (Clarity, Consistency, Feedback, Efficiency, Forgiveness, Accessibility, Hierarchy, Simplicity, Responsiveness, Delight). Includes implementation rules specific to the Padoq tech stack (Tailwind, shadcn/ui, Next.js), a design system quick reference (colors, typography, spacing, button hierarchy), mobile-specific rules, and a pre-shipping checklist.

---

### Files Created

| File | Purpose |
|------|---------|
| `Agent Guide (Please Read)/UI Design Guide/UI_UX_DESIGN_RULES.md` | Comprehensive UI/UX design rules and implementation guide |

### Files Modified

| File | Changes |
|------|---------|
| `components/routes/route-detail-drawer.tsx` | Hazards badge made clickable (scroll/view mode navigation) |
| `components/routes/routes-map-mapbox.tsx` | Hazard popup 200ms close delay with hover cancellation, route pin redesign (OS Maps style) |
| `app/routes/page.tsx` | `showHazards` default false, layer toggle works for hazards, sync toggle on view mode entry, removed back button |

---

### Risk Notes

- `showHazards: false` default means existing users who had hazards visible will need to re-enable via the toggle. This is the correct UX — hazards should be opt-in.
- Route pin color changed from `#166534` (green-800) to `#16A34A` (green-600) to match brand accent. Pins are slightly larger (32x42 vs 28x36) due to the added stroke.

### Verification

1. Click hazards badge at top of route card → enters hazards map view
2. In hazards map view, click a route pin → route detail drawer re-opens
3. Hover a hazard marker → popup appears → move mouse to popup → popup stays open → click "Mark as Cleared"
4. Map settings → Hazards toggle → toggle on → hazard markers appear on selected route
5. Map settings → Hazards toggle → default is off
6. Click hazards badge at top of route card → open map settings → hazards toggle is now ON
7. While in hazards view mode, toggle hazards OFF in settings → markers disappear, view mode exits
8. Route pins show OS Maps style: thick white outline, green fill, white center dot
