# Changelog — 2026-03-28

## Session 1

### Summary
Unified all 4 dropdown menus (main header, routes panel, routes map, mobile) into a single shared `UserDropdownMenu` component with an Airbnb-inspired clean design. Removed logo, user info header, and red logout text. Added clear section grouping, generous spacing, thin-stroke icons, and consistent styling across all pages.

### Changes

#### Shared UserDropdownMenu component
- **File created**: `components/user-dropdown-menu.tsx`
- **What changed**:
  - Single shared component renders `DropdownMenuContent` with all menu items
  - Airbnb-style design: `w-60`, `rounded-xl`, `shadow-xl`, clean section dividers
  - Items use `gap-3 px-4 py-2.5` for generous spacing
  - Icons: `h-[18px] w-[18px]` with `strokeWidth={1.5}` for thin, clean look
  - 5 clear sections separated by dividers: Notifications/Messages/Favorites, Routes/Listings/Admin, Profile/Settings/Help, Feedback/Claims, Log Out
  - No user info at top (removed name, email, role subtitle)
  - No logo at top (removed padoq branding from dropdown)
  - Log Out is plain text (removed red color)
  - Props: `user`, `unreadCount`, `notifCount`, `align`, `showRoutesLink`
  - Handles both logged-in and logged-out states
  - Contains its own logout logic (sign out + redirect)

#### Replaced 4 duplicate dropdown implementations
- **Files modified**: `components/header.tsx`, `components/routes/routes-panel-header.tsx`, `components/routes/routes-map-header.tsx`, `components/routes/mobile-top-header.tsx`
- **What changed**:
  - All 4 files now import and use `<UserDropdownMenu />` instead of inline dropdown content
  - Removed ~100 lines of duplicate menu items from each file
  - Removed unused `handleLogout` functions from all 4 files
  - Removed unused imports (`Image`, `useRouter`, icon imports) from all 4 files
  - Each file keeps its own `DropdownMenuTrigger` (different trigger styles per location)
  - Routes pages pass `showRoutesLink` prop; main header does not

### Files Modified
- `components/user-dropdown-menu.tsx` — New shared dropdown component
- `components/header.tsx` — Uses shared component, removed ~100 lines
- `components/routes/routes-panel-header.tsx` — Uses shared component, removed ~130 lines
- `components/routes/routes-map-header.tsx` — Uses shared component, removed ~140 lines
- `components/routes/mobile-top-header.tsx` — Uses shared component, removed ~130 lines

### Verification Steps
1. Main site pages (search, property, dashboard): hamburger menu opens clean Airbnb-style dropdown
2. Routes panel (left sidebar): hamburger menu opens same dropdown with "Routes" link
3. Routes map view (floating button): hamburger menu opens same dropdown with "Routes" link
4. Mobile routes view: hamburger menu opens same dropdown with "Routes" link
5. All dropdowns show notification/message counts with green dots
6. Admin users see "Admin Dashboard" in green
7. Host/admin users see "My Listings"
8. Log out works from all 4 locations
9. Logged-out state shows Sign In / Sign Up options

---

## Session 2

### Summary
Fixed crash caused by missing `LayoutDashboard` import in header.tsx (accidentally removed during dropdown refactor), and inset dropdown separator lines with `mx-4` to match Airbnb's centered divider style.

### Changes

#### Fix: LayoutDashboard crash
- **File modified**: `components/header.tsx`
- **What changed**: Restored `LayoutDashboard` import from lucide-react — it's used in the nav bar Dashboard button (line 147), not just the dropdown. Was accidentally removed during the import cleanup in Session 1.

#### Style: inset dropdown separators
- **File modified**: `components/user-dropdown-menu.tsx`
- **What changed**: Added `mx-4` to all `DropdownMenuSeparator` components so divider lines are inset from the edges, matching the Airbnb inspiration (image 4) where lines don't go fully from side to side.

### Files Modified
- `components/header.tsx` — Restored LayoutDashboard import
- `components/user-dropdown-menu.tsx` — Inset separator lines with mx-4

### Verification Steps
1. Navigate to any page — no more "LayoutDashboard is not defined" crash
2. Open dropdown menu — separator lines are centered/inset, not edge-to-edge
3. Dashboard button still visible in header for non-admin users

---

## Session 3

### Summary
Three improvements: fixed visibility card layout to fill container width using CSS grid, simplified elevation profile key points to show only start and end (removed glitchy highest/lowest), and replaced all HTML DOM pin markers with native Mapbox GL symbol layers for dramatically snappier cluster/decluster performance.

### Changes

#### Fix: Visibility cards fill container width
- **Files modified**: `components/routes/save-route-modal.tsx`, `components/routes/route-creator.tsx`
- **What changed**: Changed visibility card container from `flex justify-center gap-3` with fixed `w-[90px]` cards to `grid grid-cols-3 gap-3` with fluid-width cards. Cards now fill the available space proportionally instead of being tiny and centered.

#### Simplify: Elevation profile key points
- **File modified**: `components/routes/elevation-profile.tsx`
- **What changed**: Reduced `keyPoints` from 4-point logic (start, highest, lowest, end with de-overlap) to just start and end. Removes the visual glitchiness when multiple points overlapped on short routes.

#### Performance: Native symbol layers for route pins
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- **What changed**:
  - Added `createPinImage()` — draws the green pin icon on a canvas element
  - Pin canvas registered as a Mapbox image via `map.addImage("route-pin", canvas, { pixelRatio: 2 })` in `setupSourcesAndLayers`
  - Replaced `unclustered-point` layer from invisible circle (hit-detection only) to a native `symbol` layer using the canvas pin image
  - **Removed entire HTML marker system**: `pinMarkersRef`, `updatePinMarkers()`, the `idle`/`data` debounce timer, and all DOM element creation for pins
  - Removed the 100ms `setTimeout` sync in the silent data update path — native layers auto-sync when source data changes
  - Kept `notifyVisibleRoutes` for the panel's visible route list, now triggered on `idle` without DOM work
  - **Performance impact**: Eliminates ~200+ DOM elements being created/destroyed per zoom/pan. All pin rendering now happens on the GPU via Mapbox's WebGL pipeline. Cluster/decluster transitions are instant.

### Files Modified
- `components/routes/save-route-modal.tsx` — Grid layout for visibility cards
- `components/routes/route-creator.tsx` — Grid layout for visibility cards
- `components/routes/elevation-profile.tsx` — Start/end key points only
- `components/routes/routes-map-mapbox.tsx` — Native symbol layers, removed HTML pin system

### Verification Steps
1. Save route modal: visibility cards fill the container width in a 3-column grid
2. Route creator toolbar: same visibility card layout
3. Elevation profile: only start and end elevation labels shown (no highest/lowest)
4. Routes map: pins appear as green pin icons (same visual style as before)
5. Zoom in/out: clusters animate and split/merge snappily with no DOM lag
6. Click a pin: route preview card appears correctly
7. Click a cluster: zooms in and shows cluster route cards
8. Enter creation mode: all pins hidden; exit creation mode: pins reappear
9. Cursor changes to pointer on pin/cluster hover

---

## Session 4

### Summary
Fixed two bugs from Session 3: invisible route pins (canvas element not accepted by `map.addImage()` — now returns `{width, height, data}` format), and visibility cards still landscape-shaped (added `aspect-[3/4]` for portrait proportions).

### Changes

#### Fix: Invisible route pins
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- **What changed**: `createPinImage()` was returning an `HTMLCanvasElement`, but `map.addImage()` doesn't accept canvas elements directly. Changed return type to `{width, height, data: Uint8Array}` by extracting pixel data via `ctx.getImageData()`. Pins now render correctly as native GL symbols.

#### Fix: Visibility cards portrait aspect ratio
- **Files modified**: `components/routes/save-route-modal.tsx`, `components/routes/route-creator.tsx`
- **What changed**: Replaced `py-5` with `aspect-[3/4]` on visibility card buttons. Within the `grid grid-cols-3` container, this forces each card to be taller than it is wide (portrait orientation) matching the desired design.

### Files Modified
- `components/routes/routes-map-mapbox.tsx` — Fix `createPinImage()` return format
- `components/routes/save-route-modal.tsx` — Portrait aspect ratio on visibility cards
- `components/routes/route-creator.tsx` — Portrait aspect ratio on visibility cards

### Verification Steps
1. Routes map: green pin icons visible on the map at route start points
2. Pins render natively (no HTML DOM elements for pins)
3. Save route modal: visibility cards are portrait-shaped (taller than wide)
4. Route creator: same portrait visibility cards

---

## Session 5

### Summary
Fixed pin size and restored the classic teardrop SVG pin shape on the canvas-drawn icon. Refined the save route modal: visibility cards use `aspect-[5/6]` for squatter portrait proportions, section titles are bolder (`text-base font-semibold`), difficulty buttons are pill-shaped (`rounded-full`), Cancel is plain green text, and Save Route is a rounded pill button.

### Changes

#### Fix: Pin icon size and shape
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- **What changed**:
  - Increased `icon-size` from `0.55` to `1.0` — pins are now properly visible
  - Rewrote `createPinImage()` to reproduce the exact SVG teardrop path from the old HTML markers using `bezierCurveTo()`, matching the original `M12 0C5.4 0 0 5.4 0 12c0 9 12 20 12 20s12-11 12-20` shape with white border and inner white dot

#### Style: Save route modal refinements
- **File modified**: `components/routes/save-route-modal.tsx`
- **What changed**:
  - Visibility cards: `aspect-[3/4]` → `aspect-[5/6]` (squatter portrait, less empty space)
  - Added `px-2` and `gap-1.5` to cards for tighter content layout
  - Section labels (Route name, Description, Visibility, Difficulty, Photos): `text-sm font-medium` → `text-base font-semibold`
  - Difficulty buttons: added `rounded-full` for pill shape
  - Cancel button: `variant="outline"` → `variant="ghost"` with green text (`text-[#2E8B57]`), no border
  - Save Route button: added `rounded-full text-base`

#### Style: Route creator visibility cards
- **File modified**: `components/routes/route-creator.tsx`
- **What changed**: Same `aspect-[5/6]` + `px-2` + `gap-1.5` update as save-route-modal

### Files Modified
- `components/routes/routes-map-mapbox.tsx` — Pin size + teardrop shape
- `components/routes/save-route-modal.tsx` — Card proportions, bolder titles, rounded buttons
- `components/routes/route-creator.tsx` — Card proportions

### Verification Steps
1. Routes map: pins are visible, properly sized, classic teardrop shape with white border
2. Save route modal: visibility cards are squat portrait tiles, content fills them
3. Section titles (Route name, Description, etc.) are noticeably bolder
4. Difficulty buttons are pill-shaped (fully rounded)
5. Cancel button is plain green text with no border box
6. Save Route button is a rounded green pill
7. Route creator toolbar: visibility cards match the modal

---

## Session 6

### Summary
Replaced all map pin and cluster rendering with custom artwork and canvas-drawn cluster images. Route pins now use the designer-created PNG (`/public/Pins/route-pin.png`). Clusters use a single canvas-drawn symbol layer (dark green circle + lighter green glow + baked-in count text) instead of two separate layers, eliminating the desync fade issue. Multiple save-route-modal refinements across Sessions 5-6 (visibility card sizing, input styling, difficulty tooltip removal).

### Changes

#### Custom PNG route pins
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- **What changed**:
  - Removed `createPinImage()` canvas function
  - Pin images loaded via `map.loadImage("/Pins/route-pin.png")` and `"/Pins/property-pin.png"`
  - Registered with `pixelRatio: 2` (128x256 PNG → 64x128 logical)
  - `icon-size: 0.35` to render at appropriate map scale

#### Canvas-drawn cluster images (style A)
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- **What changed**:
  - Added `createClusterImage(count, radius)` — draws dark green circle + lighter green glow ring + white count text, all baked into one image
  - Added `ensureClusterImages(map, counts)` — generates and registers images on demand for each unique cluster count
  - Replaced two separate layers (`clusters` circle + `cluster-count` symbol) with a single `clusters` symbol layer using `["concat", "cluster-", ["get", "point_count"]]`
  - Cluster images generated dynamically on `idle` and `data` events as new counts appear
  - Single-layer approach eliminates the count-fading-separately-from-circle desync issue

#### Save route modal refinements (Sessions 5-6)
- **Files modified**: `components/routes/save-route-modal.tsx`, `components/routes/route-creator.tsx`
- Visibility cards: inline `aspectRatio: "4 / 4.5"`, `px-8` wrapper, `pt-5 gap-2` spacing, `overflow-hidden`
- Checkmark: `w-6 h-6` with `strokeWidth={3}`
- Titles: `text-base`, descriptions: `text-sm`
- Route name / description inputs: `bg-white`, char counts right-aligned below
- Difficulty tooltip removed
- Cancel: green ghost text, Save: `rounded-full`

### Files Modified
- `components/routes/routes-map-mapbox.tsx` — Custom PNG pins, canvas cluster images
- `components/routes/save-route-modal.tsx` — Multiple UI refinements
- `components/routes/route-creator.tsx` — Visibility card sizing
- `public/Pins/route-pin.png` — New custom route pin artwork
- `public/Pins/property-pin.png` — New custom property pin artwork

### Verification Steps
1. Routes map: custom horse pin icons at route start points
2. Clusters show dark green circle with lighter green glow and white count
3. Cluster → pin transition: count and circle appear/disappear together (no desync)
4. Click cluster: zooms in correctly
5. Click pin: route preview appears
6. Save route modal: all styling refinements visible

---

## Session 7

### Summary
Doubled cluster icon size for better visibility and tuned clustering parameters so route pins fade into clusters faster.

### Changes

#### Bigger cluster icons
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- **What changed**: Doubled cluster radius values in `ensureClusterImages`: `36→72, 42→84, 48→96, 54→108`. Clusters are now twice the visual size.

#### Faster pin-to-cluster transition
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- **What changed**: Increased `clusterRadius` from 60→80 (pins cluster sooner at same zoom) and lowered `clusterMaxZoom` from 12→11 (clusters dissolve one zoom level earlier). Combined effect: pins group into clusters more quickly when zooming out.

### Files Modified
- `components/routes/routes-map-mapbox.tsx` — Cluster size doubled, clustering parameters tuned

### Verification Steps
1. Routes map: clusters are visibly larger (~2x previous size)
2. Zoom out: route pins merge into clusters sooner/faster
3. Zoom in: clusters break apart at zoom 11 instead of 12
4. Cluster count text still readable and centered
5. Click cluster: still zooms in correctly

---

## Session 8

### Summary
Five map improvements: reduced cluster size to 1.5x (from 2x), added fade-in transition for route pins when clusters dissolve, fixed monochrome mode so pins and clusters stay in full colour (switches to light-v11 base style instead of CSS grayscale), and equalised property pin size to match route pins.

### Changes

#### Cluster size reduced to 1.5x
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- **What changed**: Radius values `72/84/96/108` → `54/63/72/81`. Still bigger than the original `36/42/48/54` but less dominant.

#### Route pin fade-in transition
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- **What changed**: Added `icon-opacity` paint property with zoom interpolation (`zoom 10→0, zoom 11.5→1`) to the `unclustered-point` layer. Route pins now fade in gradually as clusters dissolve instead of popping in instantly.

#### Monochrome mode: pins/clusters stay colourful
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- **What changed**: Replaced CSS `grayscale(100%)` filter (which desaturated everything including our pins) with a style-switching approach. When monochrome is on, the map switches to `light-v11` (a clean near-monochrome base style). Our custom symbol layers (pins, clusters, route lines) are re-added via the `style.load` handler and remain in full colour. Combined the separate `monochrome` and `mapType` useEffects into one.

#### Property pin size equalised
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- **What changed**: Property pin HTML marker width `42px` → `60px` to visually match route pin size (~60px rendered height from `icon-size: 0.6` at `pixelRatio: 2`).

### Files Modified
- `components/routes/routes-map-mapbox.tsx` — All four changes

### Verification Steps
1. Clusters are noticeably smaller than Session 7 but still bigger than original
2. Zoom in past clusters: route pins fade in gradually (not instant pop)
3. Toggle monochrome: base map turns light/grey, pins and clusters stay green
4. Toggle monochrome off: base map returns to outdoors style
5. Property pins and route pins appear roughly the same visual size
6. Cluster count text still readable and centered

---

## Session 9

### Summary
Removed monochrome map setting entirely (prop, state, UI toggle, implementations in both Mapbox and Google Maps components). Fixed property pin size (was too large at 60px, reduced to 28px to match route pins). Replaced broken zoom-based pin opacity with proper `icon-opacity-transition` on both cluster and pin layers for smooth cluster/pin transitions.

### Changes

#### Remove monochrome setting
- **Files modified**: `routes-map-mapbox.tsx`, `routes-map-v2.tsx`, `map-layer-controls.tsx`, `app/routes/page.tsx`
- **What changed**:
  - Removed `monochrome` prop from both map component interfaces
  - Removed monochrome from destructured props and default values
  - Removed style-switching monochrome useEffect from Mapbox component
  - Removed Google Maps monochrome styling (saturation -100)
  - Removed "Visual Settings" section with Monochrome toggle from layer controls
  - Removed unused `Palette` icon import from layer controls
  - Removed `monochrome: false` from initial layer settings state
  - Removed `monochrome={layerSettings.monochrome}` from both map component usages

#### Fix property pin size
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- **What changed**: Property pin HTML marker width `60px` → `28px` (~40px height). Was oversized after previous session's change; now matches route pin visual size.

#### Smooth cluster/pin transitions
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- **What changed**: Removed the zoom-based `icon-opacity` interpolation (which made pins permanently faded at certain zoom levels). Added `icon-opacity-transition: { duration: 600, delay: 0 }` to both `clusters` and `unclustered-point` layers. This gives a 600ms fade animation when pins/clusters appear or disappear during clustering transitions.

### Files Modified
- `components/routes/routes-map-mapbox.tsx` — Remove monochrome, fix pin size, add transitions
- `components/routes/routes-map-v2.tsx` — Remove monochrome prop and styling
- `components/routes/map-layer-controls.tsx` — Remove monochrome toggle UI and type
- `app/routes/page.tsx` — Remove monochrome state and prop passing

### Verification Steps
1. Layer controls: no "Visual Settings" / "Monochrome Map" toggle visible
2. Property pin and route pin appear the same visual size
3. Zoom in/out: clusters and pins fade in/out smoothly (600ms transition)
4. No permanent pin transparency at any zoom level

---

## Session 10

### Summary
Replaced property pin popup with a quick card at the bottom of the screen (matching the route pin UX). Clicking a property pin now zooms to it and shows a `PropertyQuickCard` with thumbnail, name, location, price, rating, and max horses. Clicking the card navigates to `/property/[id]`. Also adjusted property pin size to 40px.

### Changes

#### New PropertyQuickCard component
- **File created**: `components/routes/property-quick-card.tsx`
- **What changed**: New component matching `RouteQuickCard` layout — thumbnail (cover photo or fallback icon), property name, location, price/night in green, star rating, max horses, "Click for full details" bar at bottom. Same rounded-2xl white card with shadow, slide-in animation, close button.

#### Property pin click → zoom + quick card
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- **What changed**:
  - Added `onPropertyPreview` callback prop to interface and destructured props
  - Replaced Mapbox popup click handler with `map.easeTo()` zoom (to zoom 14) + `onPropertyPreview(property)` callback
  - Removed all inline HTML popup generation code (~40 lines)

#### Page wiring
- **File modified**: `app/routes/page.tsx`
- **What changed**:
  - Added `previewProperty` state
  - Added `handlePropertyPreview` handler (sets property, clears route preview)
  - Route preview now clears property preview (mutual exclusion)
  - Passes `onPropertyPreview={handlePropertyPreview}` to map component
  - Renders `PropertyQuickCard` at bottom when `previewProperty` is set
  - Clicking the card calls `router.push(\`/property/${id}\`)`

#### Property pin size
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- **What changed**: Property pin width adjusted to 40px to match route pin size

### Files Modified
- `components/routes/property-quick-card.tsx` — New component
- `components/routes/routes-map-mapbox.tsx` — onPropertyPreview prop, zoom + callback, pin size
- `app/routes/page.tsx` — State, handler, rendering, import

### Verification Steps
1. Click property pin: map zooms to it, quick card slides up from bottom
2. Quick card shows property photo, name, location, price, rating, horses
3. Click the card: navigates to /property/[id] page
4. Click X on card: card dismisses
5. Click a route pin while property card is showing: property card closes, route card opens
6. Click a property pin while route card is showing: route card closes, property card opens

---

## Session 11

### Summary
Three waypoint timeline fixes: gradient fade now covers the dotted timeline line, expand/collapse uses smooth height animation with auto-scroll to bottom, and duplicate waypoints after Finish are eliminated (truncation at Finish + deduplication at source).

### Changes

#### Gradient fade covers dotted timeline
- **File modified**: `components/routes/waypoint-timeline.tsx`
- **What changed**: Extended gradient overlay from `inset-x-[-10px]` to `inset-x-[-50px]` so it covers the dotted timeline line at `left-[16px]` which sits in the `pl-10` padding area.

#### Smooth expand animation + auto-scroll
- **File modified**: `components/routes/waypoint-timeline.tsx`
- **What changed**: Replaced `max-h-[5000px]` CSS hack with ref-measured `scrollHeight` for precise height animation. After expanding, auto-scrolls to the bottom of the waypoint section using `scrollIntoView({ behavior: "smooth", block: "end" })`.

#### Fix duplicate waypoints after Finish
- **File modified**: `components/routes/waypoint-timeline.tsx`
- **What changed**: Truncates the waypoint list at the Finish entry — nothing renders after it, regardless of what the data contains.
- **File modified**: `components/routes/route-detail-drawer.tsx`
- **What changed**: Added deduplication by `id` in `fullWaypointList` builder before sorting, preventing duplicate waypoints from appearing in the timeline.

### Files Modified
- `components/routes/waypoint-timeline.tsx` — Gradient fix, smooth animation, truncation at Finish
- `components/routes/route-detail-drawer.tsx` — Waypoint deduplication

### Verification Steps
1. Collapsed waypoint timeline: dotted line fades out with the gradient (not visible through it)
2. Click "Show X more waypoints": waypoints smoothly unroll with height animation
3. After expansion, view auto-scrolls to bottom of waypoint section
4. Click "Show fewer": waypoints smoothly collapse
5. No waypoints appear after the Finish entry
6. No duplicate waypoint entries in the timeline
