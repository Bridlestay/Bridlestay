# Changelog - 2026-01-21

## Session Start: ~10:00 UTC

### Summary
Continuing development on padoq platform - fixing logo, routes, photos, and UI improvements after rebrand.

---

### 1. Logo and Header Fix

**Why:** Logo wasn't displaying correctly after rebrand. The word "padoq" was showing redundantly alongside the logo.

**Files modified:**
- `components/header.tsx`
- `public/logo.png` (renamed from `logo.png.png`)

**Changes:**
- Removed the text "padoq" from header, showing only the logo image
- Renamed logo file from `logo.png.png` to `logo.png` (fixed double extension)
- Increased logo size in header for better visibility (120x40)

---

### 2. Cookie Preferences in Account Settings

**Why:** Users reported having to choose cookie options every time they visit. Added ability to change preference in settings.

**Files modified:**
- `app/account/edit/page.tsx`

**Changes:**
- Added Cookie Preferences section to account edit page
- Users can now toggle analytics/performance cookies on/off
- Settings persist to localStorage with `padoq-cookie-consent` key
- Shows current preference state with toggle switch

**Verification:**
- Go to Account → Edit Profile
- Scroll to "Cookie Preferences" section
- Toggle should reflect current localStorage value

---

### 3. Route Delete Fix (Using Service Role)

**Why:** Routes were failing to delete due to statement timeout (RLS policies were slow).

**Files modified:**
- `app/api/routes/[id]/route.ts`

**Changes:**
- Added service role client (`getAdminSupabase()`) for delete operations
- Changed to parallel deletion of related records using `Promise.all`
- Bypasses RLS for faster deletion while still checking authorization first
- Tables deleted in parallel: comments, photos, waypoints, likes, favorites, hazards, shares, completions

**Risk notes:**
- Uses SUPABASE_SERVICE_ROLE_KEY (must be set in environment)
- Authorization check happens first before any deletion

---

### 4. Photos Section Improvements

**Why:** Photo organization was confusing. Needed clear distinction between Display, Author, and User photos with proper management actions.

**Files modified:**
- `components/routes/route-detail-drawer.tsx`

**Changes:**
- "Display Photos" section now always visible (shows empty state if none)
- For non-owners/admins, section is labeled "Route Photos" instead of "Display Photos"
- Renamed "Community Photos" to "User Photos"
- All photo cards now show action menu on hover (Set as Cover, Add to Display, Delete)
- Added empty state message for display photos with guidance

---

### 5. Display Photos Carousel in Route Header

**Why:** Users wanted to see featured/display photos prominently when opening a route.

**Files modified:**
- `components/routes/route-detail-drawer.tsx`

**Changes:**
- Added photo carousel between description and metadata
- Shows cover photo first, followed by display photos
- Horizontally scrollable with snap points
- Shows "Cover" badge on cover photo
- Dot indicators for multiple photos

---

### 6. Difficulty Display Fix

**Why:** Difficulty was only showing a color in some cases, not the difficulty name.

**Files modified:**
- `components/routes/route-detail-drawer.tsx`
- `components/routes/route-card.tsx`

**Changes:**
- Expanded difficulty configuration to include all possible values:
  - unrated (gray)
  - easy (green)
  - moderate (amber)
  - medium (amber)
  - difficult (orange)
  - hard (red)
  - severe (dark red)
- Each difficulty now has both a color class and a proper display label
- Created `getDifficultyInfo()` helper function for consistent mapping
- Badge now shows proper capitalized label (e.g., "Moderate" not "moderate")

**Verification:**
- View any route card - should show difficulty name in colored badge
- Open route details - should show proper difficulty name

---

### 7. Route Delete Function (Fix Timeout Issue)

**Why:** Routes were failing to delete due to statement timeout even with service role (RLS bypass).

**Files created:**
- `supabase/migrations/058_route_delete_function.sql`

**Files modified:**
- `app/api/routes/[id]/route.ts`

**Changes:**
- Created `delete_route_cascade(UUID)` PostgreSQL function with 60s timeout
- Function deletes all related records then the route itself
- API now calls the RPC function first, with fallback to manual deletion
- Runs as SECURITY DEFINER with elevated privileges

**Migration required:**
Run migration 058 in Supabase SQL Editor to enable the function.

---

### 8. Photos Section Reorganization (Session 2)

**Why:** User reported issues with photo sections not displaying correctly and missing empty states.

**Files modified:**
- `components/routes/route-detail-drawer.tsx`

**Changes:**
- Added state variables for categorized photos (coverPhoto, apiDisplayPhotos, authorPhotos, userPhotos)
- fetchPhotos now stores all categories from API response
- **Display Photos** section: only shows for authors/admins
- **Route Photos** section: only shows for non-authors (displays the featured photos)
- **Author Photos** section: always shows with empty state message
- **User Photos** section: always shows with "Currently no user photos have been added" when empty
- Fixed photo carousel to use `displayPhotosForCarousel` correctly
- All PhotoCard components now refresh all photo states on update

---

### 9. Fixed Route Search and Cover Photos

**Why:** Public routes weren't loading (search timing out) and route cards showed grey placeholders instead of cover images.

**Files modified:**
- `app/api/routes/search/route.ts`
- `components/routes/route-card.tsx`

**Changes:**
- Search now uses service role client to bypass slow RLS queries
- Explicitly selects only needed columns (no heavy joins)
- Fetches cover photos in separate query after getting routes
- Returns `cover_photo_url` field for each route
- Route card now uses `cover_photo_url` with fallback to first photo
- Photos fetched separately with `is_cover=true` check, fallback to first photo

**Verification:**
- Public routes should now load on Explore Routes page
- Route cards should show cover images (or first photo if no cover set)

---

### 10. Photo Categorization Error Handling

**Why:** Setting cover/display wasn't showing proper errors when RLS blocked updates.

**Files modified:**
- `app/api/routes/[id]/photos/route.ts`
- `components/routes/route-detail-drawer.tsx`

**Changes:**
- Added detailed logging for photo PATCH operations
- Better detection of column existence
- PhotoCard handlers now check response status and show migration/error messages
- Toast now shows specific error if RLS blocks update

---

### 11. Removed "Verified" Badge from Route Creator

**Why:** User requested no mention of users being verified anywhere on the site.

**Files modified:**
- `components/routes/route-detail-drawer.tsx`

**Changes:**
- Removed the `{route.owner.admin_verified && <Badge>Verified</Badge>}` from route creator display

---

### 12. Improved Waypoint Map Picker UI

**Why:** Map was too small and didn't prominently show the route line.

**Files modified:**
- `components/routes/waypoint-map-picker.tsx`
- `components/routes/route-detail-drawer.tsx`

**Changes:**
- Dialog now uses `max-w-4xl w-[90vw]` for much wider display
- Map height increased to `h-[50vh] min-h-[400px]`
- Updated description to clarify clicking on green route line

---

### 13. Routes Search Optimization

**Why:** Public routes search was timing out even with service role.

**Files created:**
- `supabase/migrations/059_routes_search_function.sql`

**Files modified:**
- `app/api/routes/search/route.ts`

**Changes:**
- Created `search_public_routes()` PostgreSQL function with 30s timeout
- API now tries RPC function first, falls back to simple query
- "My Routes" uses separate simpler query path
- Cover photos fetched efficiently in separate query

**Migration required:** Run migration 059 for optimal performance.

---

### 14. Added Edit Route Page

**Why:** Route authors need ability to edit their routes.

**Files created:**
- `app/routes/edit/[id]/page.tsx`

**Files modified:**
- `components/routes/route-detail-drawer.tsx`

**Changes:**
- Added "Edit Route" button in route detail drawer (visible to owners/admins)
- Created edit page at `/routes/edit/[id]`
- Allows editing: title, description, difficulty, visibility
- Note: Path editing requires creating new route (complex)

---

*Changelog created by AI Assistant (Claude)*
*Last updated: 2026-01-21 ~12:00 UTC*
