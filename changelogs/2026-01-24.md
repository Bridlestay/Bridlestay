# Changelog - 2026-01-24

## Summary
Major fixes for route features: explore routes filtering, route completion, waypoint permissions, edit route functionality, and various UI improvements.

---

### 1. Fixed Explore Routes Showing External Bridleways

**Why:** Explore routes page was showing 283,485 imported bridleways/byways instead of only user-created routes.

**Files modified:**
- `app/api/routes/search/route.ts`
- `supabase/migrations/060_fix_route_features_jan24.sql`

**Changes:**
- Added `NOT owner_user_id IS NULL` filter to exclude imported external data
- Updated `search_public_routes` database function to only return user-created routes
- Imported bridleways/byways have `owner_user_id = null` and are now filtered out

---

### 2. Fixed Route Completion API (500 Error)

**Why:** Users couldn't mark routes as complete - getting 500 Internal Server Error.

**Files created:**
- `supabase/migrations/060_fix_route_features_jan24.sql`

**Changes:**
- Fixed RLS policies for `route_completions` table
- Added explicit policies for INSERT, SELECT, UPDATE, DELETE
- Any authenticated user can now mark routes as complete

**Migration required:** Run migration 060 in Supabase SQL Editor.

---

### 3. Allowed Non-Authors to Add Waypoints

**Why:** Only route authors could add waypoints. Now any authenticated user can contribute waypoints to help others navigate.

**Files modified:**
- `app/api/routes/[id]/waypoints/route.ts`
- `supabase/migrations/060_fix_route_features_jan24.sql`

**Changes:**
- Removed ownership check from waypoint POST endpoint
- Added `created_by_user_id` column to track who created each waypoint
- Updated RLS policies to allow any authenticated user to INSERT waypoints
- Users can still only edit/delete their own waypoints (or route owner/admin)

---

### 4. Fixed Waypoint Map Picker

**Why:** Route line wasn't showing when adding waypoints; dialog was too tall.

**Files modified:**
- `components/routes/waypoint-map-picker.tsx`
- `components/routes/route-detail-drawer.tsx`

**Changes:**
- Added debugging logs to trace geometry parsing issues
- Added support for stringified JSON geometry format
- Added `max-h-[90vh] overflow-y-auto` to dialog to prevent stretching off screen
- Map height changed to `h-[40vh] min-h-[300px] max-h-[500px]` for better fit
- Changed waypoint button to show for ALL authenticated users (not just owners)

---

### 5. Full Route Editing with Same UI as Creation

**Why:** Users needed to be able to edit route path, not just metadata.

**Files modified:**
- `app/routes/edit/[id]/page.tsx` (complete rewrite)
- `app/api/routes/[id]/route.ts`

**Changes:**
- Edit page now uses the full `RouteCreator` component with map
- Users can modify the route path by adding/moving/removing waypoints
- Same toolbar, layer toggles, and UI as route creation
- API PATCH endpoint now accepts geometry, visibility, route_type updates
- Admins can also edit any route

---

### 6. Added `last_edited_at` Timestamp to Routes

**Why:** Track when routes were last modified for user visibility.

**Files created:**
- `supabase/migrations/060_fix_route_features_jan24.sql`

**Changes:**
- Added `last_edited_at` column to routes table
- Created trigger to auto-update timestamp on any route modification
- Edit page shows "Last edited: {date}" when available

---

### 7. Added Debug Logging for Geometry Issues

**Why:** Help diagnose why route line might not appear in waypoint map.

**Files modified:**
- `components/routes/waypoint-map-picker.tsx`
- `components/routes/route-detail-drawer.tsx`

**Changes:**
- Console logs now show geometry type and parsing results
- Logs indicate if geometry is missing, its type, and coordinate count

---

### 8. Fixed Next.js Dynamic Route Parameter Conflict

**Why:** Server failed to start with error: `You cannot use different slug names for the same dynamic path ('id' !== 'propertyId')`.

**Files modified:**
- `app/api/reviews/property/[propertyId]/route.ts` → renamed to `app/api/reviews/property/[id]/route.ts`
- `app/api/questions/[propertyId]/route.ts` → renamed to `app/api/questions/[id]/route.ts`

**Changes:**
- Renamed dynamic route folders from `[propertyId]` to `[id]` to match convention across the codebase
- Updated route handlers to use `params.id` instead of `params.propertyId`
- This ensures all API routes under `app/api` use consistent parameter naming

---

### 9. Fixed Edit Route Page TooltipProvider Error

**Why:** Clicking "Edit Route" caused a crash with error: `'Tooltip' must be used within 'TooltipProvider'`

**Files modified:**
- `app/routes/edit/[id]/page.tsx`

**Changes:**
- Added `TooltipProvider` import and wrapped the page content
- `RouteCreatorToolbar` uses Tooltip components which require this provider context

---

### 10. Fixed WaypointMapPicker Not Showing Route Line

**Why:** When adding waypoints, the green route line wasn't visible on the map despite the geometry being present.

**Files modified:**
- `components/routes/waypoint-map-picker.tsx`

**Changes:**
- Added `routeGeometry` as direct dependency to the draw effect so it re-runs when geometry becomes available
- Added `zIndex: 1000` to ensure route polyline renders on top of other map elements
- Added console logging for debugging route drawing
- Fixed effect to properly wait for both map and geometry to be ready

---

### 12. Added Average Rating from Route Completions

**Why:** Users can rate routes when marking them complete, but this wasn't updating the route's average rating.

**Files created:**
- `supabase/migrations/061_route_completion_ratings.sql`

**Changes:**
- Created trigger `update_route_completion_rating_trigger` on `route_completions` table
- Automatically updates `routes.avg_rating` and `routes.review_count` when completions are added/updated/deleted
- Updates existing routes with completion ratings
- Added storage bucket policies for route photo uploads

---

### 13. Fixed Cover Images Not Showing on Explore Routes

**Why:** Public routes weren't showing cover images on route cards.

**Files modified:**
- `app/api/routes/search/route.ts`

**Changes:**
- Added cover photo fetching for public routes (was only done for "My Routes")
- Creates a cover photo map and attaches `cover_photo_url` to each route in response

---

## Migration Required

Run the following migrations in Supabase SQL Editor:

**1. `supabase/migrations/060_fix_route_features_jan24.sql`**

This migration:
- Adds database index for faster public route search queries
- Adds `last_edited_at` column with auto-update trigger
- Adds `created_by_user_id` to route_waypoints
- Fixes RLS policies for waypoints and completions
- Updates `search_public_routes` function to exclude imported bridleways

**2. `supabase/migrations/061_route_completion_ratings.sql`**

This migration:
- Adds trigger to update `avg_rating` and `review_count` on routes from completion ratings
- Updates existing routes with their completion ratings
- Adds storage bucket policies for route photo uploads

---

*Changelog created by AI Assistant (Claude)*
*Date: 2026-01-24*

