# Changelog - 2026-03-05

## Session 1: Edit Waypoint View Visual Overhaul

**Summary:** Redesigned the edit waypoint view to match the route card's visual style instead of rendering as a plain, smaller form.

---

### Changes Made

#### 1. Redesign Edit Waypoint View

**Modified Files:**
- `components/routes/edit-waypoint-view.tsx`
- `components/routes/route-detail-drawer.tsx`

**What Changed:**

**edit-waypoint-view.tsx:**
- Replaced plain `flex flex-col h-full` layout with route-card-matching structure
- Added green gradient header banner (matching photo hero area) with back button, MapPin icon, title, and waypoint name
- Content area uses `rounded-t-2xl -mt-3` overlay pattern (same as route card content over photo)
- Form fields have matching `p-4 pt-6 space-y-5` padding (consistent with route card content area)
- Removed inline Cancel/Save footer - actions now in the modal's sticky bottom bar
- Form uses `<form id="edit-waypoint-form">` element so external submit button can trigger it
- Back button styled as circular translucent pill (matching route card close button style)

**route-detail-drawer.tsx:**
- Hidden the Save/Navigate bottom bar when `editingWaypointId` is set
- Added dedicated edit action bar with Cancel + Save Changes buttons in the sticky bottom bar
- Save button uses `form="edit-waypoint-form"` to submit the form from outside the scroll area

---

## Session 2: Edit View Improvements + Map Toggle Fix

**Summary:** Fixed edit view sizing to match route card photo hero, added photo upload to waypoint editing, hid edit on Start/Finish waypoints, and fixed View All waypoint map toggle.

---

### Changes Made

#### 1. Edit View Sizing + Photo Upload

**Modified Files:**
- `components/routes/edit-waypoint-view.tsx` (rewritten)
- `components/routes/route-detail-drawer.tsx` (pass routeId)

**What Changed:**
- Header now uses `h-48 md:h-56` (192px/224px) matching route card no-photo placeholder height
- Header displays waypoint's first photo as background (like route card photo hero) with overlay
- If no photos: shows MapPin icon placeholder (same style as route card no-photo state)
- Back button styled identically to route card close button (top-left, `bg-black/40`)
- "Add photo" button in bottom-right of hero area (matching "See all" photo button position)
- Photo count badge in bottom-left
- Photos section below form fields showing 3-column grid with delete buttons on hover
- Full photo upload flow: file picker, validation (10MB, image only), upload to API, instant state update
- Photo delete with confirmation
- `routeId` now passed as prop from drawer for API calls

#### 2. Hide Edit on Start/Finish Waypoints

**Modified Files:**
- `components/routes/waypoint-timeline.tsx`

**What Changed:**
- Added `!isStart && !isFinish` conditions to `onEdit` and `onSuggestEdit` callbacks
- Start and Finish are virtual waypoints (derived from route geometry), not DB records
- Prevents showing edit/suggest buttons that would fail on these waypoints

#### 3. View All Waypoint Map Toggle

**Modified Files:**
- `app/routes/page.tsx`

**What Changed:**
- `handleEnterViewMode("waypoints")` now sets `showWaymarkers: true` in layer settings
- Previously only hazards mode synced its toggle (`showHazards: true`)
- Added exit effect: waypoints view mode auto-exits when `showWaymarkers` is toggled off in settings
- Matches existing hazards pattern for symmetry

#### 4. Owner Photo Upload API Fix

**Modified Files:**
- `app/api/routes/[id]/waypoints/[waypointId]/photos/route.ts`

**What Changed:**
- Route owner can now upload photos to their own waypoints without completing the route
- Previously ALL uploads required route completion, blocking the owner from adding photos during edit
- Non-owners still require route completion (community photos)

---

#### 5. Fix Waypoint Save + Update API URL

**Modified Files:**
- `components/routes/edit-waypoint-view.tsx`
- `app/api/routes/[id]/waypoints/[waypointId]/route.ts`

**What Changed:**
- Fixed PATCH URL from `/api/waypoints/${id}` (non-existent) to `/api/routes/${routeId}/waypoints/${id}` (actual endpoint)
- This was why saving waypoint edits silently failed (404 response)
- Also fixed PATCH endpoint to accept `null` for `icon_type` and `tag` fields (used `!== undefined` instead of truthy check)

---

#### 6. Fix Waypoint Photo Upload 500 Error

**Modified Files:**
- `app/api/routes/[id]/waypoints/[waypointId]/photos/route.ts`
- `supabase/migrations/075_fix_waypoint_photos_fk.sql` (NEW)

**What Changed:**
- The `waypoint_photos` table FK on `user_id` referenced `auth.users(id)` instead of `public.users(id)`
- PostgREST join `users!waypoint_photos_user_id_fkey` failed because it couldn't traverse from `auth.users` FK to `public.users` table
- Removed explicit FK hint from join queries (use `user:users(...)` instead)
- Migration 075 drops the bad FK and re-creates it pointing to `public.users(id)`

**Risk:** Migration must be run in production Supabase dashboard.

---

### SQL Migrations to Run
```sql
-- Migration 075: Run in Supabase SQL Editor
ALTER TABLE waypoint_photos
  DROP CONSTRAINT IF EXISTS waypoint_photos_user_id_fkey;

ALTER TABLE waypoint_photos
  ADD CONSTRAINT waypoint_photos_user_id_fkey
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL;
```

---

### Verification
- [x] `npm run build` passes
- [ ] Edit view hero matches route card dimensions
- [ ] Waypoint edits save successfully
- [ ] Photos can be uploaded and displayed in edit view (after running migration 075)
- [ ] Photos can be deleted from edit view
- [ ] Start/Finish waypoints do not show edit/suggest buttons
- [ ] "View all" button toggles waypoint markers on in map settings
- [ ] Toggling waypoints off in settings exits waypoints view mode
