# Changelog — 2026-03-13

## Session 1: Fix route edit saving as new route + no-photo rounded corners

### Summary
Fixed two issues: (1) Editing a route created a new route instead of updating because the SaveRouteModal kept stale form state between uses, and (2) the "No photos yet" placeholder in the route detail modal had no visible rounded corners.

### Files Modified
- `app/routes/page.tsx` — Added `key` prop to `SaveRouteModal` to force remount between create/edit modes
- `components/routes/route-detail-drawer.tsx` — Inset the "No photos yet" placeholder with padding and rounded corners

### What Changed and Why

**Route edit creating new routes (Issue 1)**:
- **Root cause**: The `SaveRouteModal` component uses `useState` to initialize form fields from `existingData` props. However, `useState` initializers only run on first mount. Since the modal stays mounted (returns `null` when closed rather than being removed from the tree), switching between create and edit modes left stale form state — empty fields, wrong button text, and incorrect save behavior.
- **Fix**: Added a `key` prop (`key={isEditing ? 'edit-${editingRouteId}' : 'create'}`) to the `SaveRouteModal`. When the key changes (switching between create/edit or editing a different route), React unmounts and remounts the component, ensuring `useState` initializers run fresh with the correct `existingData`. This means the form pre-populates with existing title, description, visibility, and difficulty when editing, and the button correctly shows "Update Route".

**No-photo rounded corners (Issue 2)**:
- **Root cause**: The nested `overflow-y-auto` scrollable div inside the card prevented the card's `rounded-2xl overflow-hidden` from visually clipping the green gradient at the top corners.
- **Fix**: Added `rounded-t-2xl` directly to the "No photos yet" gradient div so it matches the card's corner radius. The gradient stays edge-to-edge (same layout as the photo carousel) but now has properly rounded top corners.

### Verification Steps
1. `npm run build` passes
2. **Edit existing route**: Open route detail → three-dot menu → Edit → modify route → Save Route → modal shows "Update Route" button with pre-filled title/description → saves correctly as update (no new route created)
3. **Create new route**: Start fresh route → Save Route → modal shows "Save Route" button with empty fields → creates new route
4. **No-photo styling**: Open a route with no photos → white content area below the green gradient has rounded top corners overlapping the hero, same as routes with photos
5. **Photo styling**: Routes with photos still show edge-to-edge photo carousel (no change)

---

## Session 2: Fix no-photo rounded corners (correct approach) + deterministic undo

### Summary
Fixed the rounded corners on the "No photos yet" route detail card (the white content area below the hero now has the same `rounded-t-2xl -mt-4` pull-up effect as routes with photos). Also made undo deterministic by saving snapped segments in the undo history — undo now restores the exact previous route geometry instead of re-fetching from Mapbox.

### Files Modified
- `components/routes/route-detail-drawer.tsx` — Apply `rounded-t-2xl -mt-4` to content area for all routes (not just photos)
- `components/routes/routes-map-mapbox.tsx` — Added `getSnappedSegments` and `restoreSnappedSegments` methods to map ref
- `app/routes/page.tsx` — Changed undo history to store `{ waypoints, segments }` snapshots; undo restores both directly

### What Changed and Why

**No-photo rounded corners (corrected)**:
- **Root cause**: The content area below the photo hero (`<div className="relative z-10 bg-white">`) only got `rounded-t-2xl -mt-4` when photos were present (`displayPhotosForCarousel.length > 0`). Without photos, the content had no rounded top corners and no negative margin overlap.
- **Fix**: Apply `rounded-t-2xl -mt-4` and `pt-6` unconditionally to the content area, so both photo and no-photo routes have the same card pull-up visual.

**Deterministic undo with snapped segments**:
- **Root cause**: Undo cleared all snapped segments and triggered a full re-fetch from Mapbox Directions API. The re-fetched segments could produce different road-following geometry than what the user had before, causing the route to "jank" to different roads.
- **Fix**: The undo history now stores a deep copy of the snapped segments alongside waypoints. On undo, both are restored directly — no Mapbox API calls, instant, and the route looks exactly as it did before the last change. This works correctly regardless of whether snap was on or off at the time, or whether the user has since toggled snap mode.

### Risk Notes
- History entries are slightly larger (include segment coordinate arrays). For a typical route with 20 undo steps, this adds ~50-100KB — negligible.
- `getSnappedSegments()` returns a deep copy so mutations to the live segments don't affect stored history.

### Verification Steps
1. `npm run build` passes
2. **No-photo rounded corners**: Open a route with no photos → white content area has rounded top corners overlapping the green gradient, matching the photo layout
3. **Undo with snap on**: Place 4+ waypoints with snap enabled → undo → route instantly reverts to exact previous geometry (no re-fetch, no janking to different roads)
4. **Undo across snap toggle**: Place points with snap on → toggle snap off → undo → route restores with snapped geometry from when it was snapped
5. **Multiple undo**: Undo several times in succession → each step restores the exact previous state
6. **Undo after erase**: Erase a waypoint → undo → route restores with correct segments

---

## Session 3: Fix undo capturing post-mutation segments for erase/drag

### Summary
Undo after erasing a middle waypoint or dragging a waypoint didn't fully restore the snapped route because the segments snapshot was taken AFTER the erase/drag handler had already mutated them. Fixed by snapshotting segments before any mutation and passing the pre-mutation snapshot through to the history.

### Files Modified
- `components/routes/routes-map-mapbox.tsx` — Added `snapshotSegments()` helper; erase and drag handlers now snapshot segments before mutating and pass the snapshot to callbacks
- `app/routes/page.tsx` — `pushHistory`, `removeWaypoint`, and `updateWaypoint` now accept an optional `segmentsSnapshot` parameter for pre-mutation state

### What Changed and Why
- **Root cause**: The erase handler calls `reindexSegmentsOnRemove()` which mutates `snappedSegmentsRef` in place. Then it calls `onWaypointRemove()` → `pushHistory()` → `getSnappedSegments()`, which reads the already-mutated segments. Same issue for drag handlers that clear adjacent segments before calling `onWaypointUpdate()`.
- **Fix**: Both handlers now call `snapshotSegments(snappedSegmentsRef.current)` BEFORE any mutation. The snapshot is passed through the callback chain to `pushHistory(segmentsSnapshot)`, which stores it in the undo history instead of reading current (mutated) state.

### Verification Steps
1. `npm run build` passes
2. **Undo after erase middle point**: Place 3+ waypoints with snap on → erase a middle one → undo → route fully restores with original snapped geometry (not straight lines)
3. **Undo after drag**: Drag a waypoint to new position → undo → route restores with original snapped geometry
4. **Undo after adding point**: Place a waypoint → undo → works as before (no regression)

---

## Session 4: Mobile route experience redesign

### Summary
Redesigned the mobile route viewing experience: route detail drawer is now full-screen on mobile (Komoot-style), bottom nav and FAB animate out when route cards appear, and the quick card now uses a swipeable peek carousel for browsing clustered routes.

### Files Modified
- `components/routes/route-detail-drawer.tsx` — Full-screen sheet on mobile (`h-[100dvh]`), centered modal kept for desktop
- `components/routes/mobile-bottom-nav.tsx` — Added `visible` prop with slide-down animation (`translate-y-full`)
- `components/routes/mobile-fab-menu.tsx` — Added `visible` prop with fade-out + slide-down animation
- `components/routes/route-quick-card.tsx` — Rewritten as swipeable peek carousel (CSS scroll-snap, 85% width cards with adjacent peek, dot indicators) for clustered routes; single routes keep centered card style
- `app/routes/page.tsx` — Orchestrated mobile state: nav/FAB hide when quick card or drawer visible, carousel props passed to quick card, removed stale mobile panel toggle for route detail

### What Changed and Why

**Full-screen route detail on mobile**:
- Mobile: `h-[100dvh]` full-screen sheet sliding up from bottom, no rounded corners, no backdrop (drawer covers everything)
- Desktop: unchanged centered modal with `max-w-2xl max-h-[80vh]`, rounded corners, backdrop
- Uses responsive breakpoints (`md:`) to split behaviour cleanly

**Bottom nav + FAB hide/show**:
- Both components accept a `visible` prop (default `true`)
- Bottom nav slides down with `translate-y-full` transition (300ms ease-in-out)
- FAB fades out + slides down with `opacity-0 translate-y-8` transition
- Hidden when quick card is showing OR when full detail drawer is open

**Swipeable peek carousel**:
- Single route: centered card at 92% width (unchanged visual)
- Multiple routes (cluster click): CSS `scroll-snap` carousel, each card is 85% viewport width so adjacent cards peek ~7.5% on each side
- Scroll-snap with `snap-center` for natural swipe behaviour
- Dot indicators below carousel (green active dot, gray inactive)
- On desktop: falls back to showing one card at a time (hides non-active cards)
- `onIndexChange` callback syncs carousel swipe with map route highlighting

**State flow**:
- Map → click pin → quick card slides up, nav/FAB slide down
- Swipe carousel → map updates drawn route
- Tap card → full detail slides up (full-screen on mobile)
- X on detail → dismiss → quick card restored, detail gone
- X on quick card → everything cleared, nav/FAB return

---

## Session 5: Mini card X button fix + Find/Saved panels full-screen on mobile

### Summary
Fixed the carousel close button appearing detached from cards by moving it inside each card. Made Find Routes and Saved Routes panels full-screen on mobile (same treatment as route detail drawer). Bottom nav now hides when these panels are open.

### Files Modified
- `components/routes/route-quick-card.tsx` — Moved X button inside `SingleCard` component (top-right corner of each card), removed floating external close button
- `components/routes/find-routes-panel.tsx` — Full-screen `h-[100dvh]` sheet on mobile, centered modal on desktop, backdrop hidden on mobile
- `components/routes/saved-routes-panel.tsx` — Same full-screen treatment as Find Routes panel
- `app/routes/page.tsx` — Bottom nav hides when Find/Saved tabs are active, FAB only shows on map tab

### What Changed and Why

**Mini card X button**:
- Previously the X floated above the carousel at a fixed position, visually detached from the active card (especially noticeable when swiping between cards in a cluster)
- Moved into `SingleCard` component as `onClose` prop — each card now has its own X in the top-right corner, travelling with the card as you swipe

**Find Routes + Saved Routes full-screen**:
- Both panels used the same centered modal pattern (`max-w-2xl max-h-[85vh]`) that didn't fill the mobile screen
- Applied identical responsive treatment as route detail: `h-[100dvh]` full-screen on mobile, centered `md:max-w-2xl md:max-h-[80vh]` on desktop
- Desktop backdrop preserved, mobile backdrop hidden (panels cover everything)
- Header `rounded-t-2xl` only applies on desktop (`md:rounded-t-2xl`)

**Nav/FAB visibility**:
- Bottom nav now hides when Find or Saved tabs are active (panels are full-screen, no need for nav)
- FAB only shows on map tab (simplified logic)

### Verification Steps
1. `npm run build` passes
2. **Mini card X**: Click a cluster pin → swipe between cards → X appears on each card's top-right, moves with swipe
3. **Find Routes mobile**: Tap Find tab → panel fills entire screen, slides up from bottom, no rounded corners
4. **Saved Routes mobile**: Tap Saved tab → same full-screen behaviour
5. **Desktop unchanged**: Both panels show as centered modals with backdrop on desktop
6. **Nav hides**: Open Find/Saved → bottom nav slides down; close panel → nav returns

### Verification Steps
1. `npm run build` passes
2. **Mobile full-screen detail**: Open route detail on mobile → fills entire screen, no rounded corners, slides up from bottom
3. **Desktop unchanged**: Route detail on desktop → centered modal with backdrop, rounded corners
4. **Nav hides on quick card**: Click a map pin on mobile → bottom nav slides down, FAB fades out
5. **Nav returns on close**: Close quick card → nav slides back up, FAB fades in
6. **Carousel swipe**: Click a cluster of routes → swipe between cards, adjacent cards peek on edges
7. **Dot indicators**: Dots appear below carousel, active dot is green and wider
8. **Tap carousel card → full detail**: Tap the active card → full-screen detail opens
9. **Dismiss detail → quick card**: Press X on detail → slides down, quick card reappears

---

## Session 6: Cluster index preservation + mobile creation toolbar reorder

### Summary
Fixed carousel resetting to wrong route when zooming out (cluster reforms), reordered mobile route creation toolbar to match desired UX, and hid bottom nav/FAB during route creation.

### Files Modified
- `app/routes/page.tsx` — Cluster browse index preserves currently viewed route on zoom out; bottom nav and FAB hidden during route creation (`!isCreating`)
- `components/routes/route-creator.tsx` — Mobile toolbar button order changed to: Plot, Erase, Waypoint, Snap, Undo, Clear, Exit; "Remove" label renamed to "Erase"; Exit button added to mobile layout

### What Changed and Why

**Cluster index preservation**:
- When zooming in on a clustered route then zooming back out, `handleClusterClick` always reset `clusterBrowseIndex` to 0, showing the first route instead of the one the user was viewing
- Fixed by checking if the currently previewed route exists in the new cluster and preserving its index

**Mobile creation toolbar reorder**:
- Previous order: Plot, Snap, Undo, Remove, Waypoint (conditional), Clear
- New order: Plot, Erase, Waypoint, Snap, Undo, Clear, Exit
- "Remove" renamed to "Erase" for consistency with desktop toolbar
- Exit button (X icon) added to mobile toolbar — previously only available on desktop

**Hide nav during creation**:
- Bottom nav and FAB now hide when `isCreating` is true, giving the map full screen space for route plotting on mobile

### Verification Steps
1. `npm run build` passes
2. **Cluster index**: Zoom into a cluster → view a specific route → zoom out → carousel shows the same route you were viewing
3. **Mobile toolbar order**: Enter route creation on mobile → toolbar shows Plot, Erase, Waypoint, Snap, Undo, Clear, Exit (left to right)
4. **Exit button**: Tap Exit on mobile toolbar → exits route creation mode
5. **Nav hides during creation**: Enter route creation on mobile → bottom nav slides down, FAB fades out; exit creation → both return
