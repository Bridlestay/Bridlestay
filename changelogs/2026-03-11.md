# Changelog — 2026-03-11

## Session 1: Route creation UX improvements (6 fixes)

### Summary
Six improvements to route creation and map interaction: cluster navigation scoped to clicked cluster, route pins hidden during creation, cursor improvements, zoom-aware waypoint snap radius, waypoint numbering by proximity to start, and waypoint editing after placement.

### Files Modified
- `app/routes/page.tsx` — Disabled visible routes expansion in cluster browse, added `distanceAlongCoords` helper, sorted POIs by route distance before saving with `order_index`, added POI edit state + handlers
- `components/routes/routes-map-mapbox.tsx` — Hide/restore route pins and cluster layers on creation mode toggle, added per-tool cursors (crosshair/not-allowed/copy), added `distanceAlongRoute` helper, sorted POI marker numbering by route distance, zoom-aware snap tolerance (50m–200m), added `onCreationPOIEdit` callback for clicking POI markers
- `components/routes/quick-add-waypoint-dialog.tsx` — Added edit mode: `editingWaypoint` and `onUpdate` props, pre-fills form, shows "Edit Waypoint" / "Save Changes" labels

### What Changed and Why

**1. Cluster navigation scope**: Previously, after clicking a cluster, the navigation expanded to include all visible unclustered pins as you zoomed/panned. This was confusing — you'd see "3 of 12" when you only clicked a cluster of 3. Now cluster browse is locked to the originally clicked cluster's routes.

**2. Hide pins during creation**: Existing route pins and cluster circles stayed visible when entering creation mode, cluttering the map. Now all pin markers are removed and cluster layers hidden when `isCreating` becomes true, and restored when leaving creation mode.

**3. Cursor per tool mode**: Plot mode now shows `crosshair`, erase shows `not-allowed`, insert shows `copy`. Previously only plot had a crosshair but it could be overridden by hover events on the now-hidden pin layers.

**4. Zoom-aware waypoint snap**: The snap tolerance for placing waypoints on the route line was fixed at 50m. At low zoom levels, this made it very hard to click on the line. Now: 50m at zoom ≥14, 100m at zoom ≥12, 200m below that.

**5. Waypoint numbering by proximity**: POI waypoints were numbered W1, W2, W3 etc. by creation order. Now they're numbered by distance along the route from start. If you add a waypoint near the beginning after already adding one near the end, it correctly becomes W1. This applies both to map marker labels (during creation) and to `order_index` when saving to the database.

**6. Edit waypoints after placing**: Clicking a POI marker during creation (in any mode except erase) now opens the quick-add dialog pre-filled with the waypoint's name, tag, icon, and description. Changes are saved back to the creation state.

### Verification Steps
1. `npm run build` passes
2. **Cluster nav**: Click a cluster → navigate only those routes, not others visible on map
3. **Hidden pins**: Click Create Route → all existing route pins and clusters disappear
4. **Cursor**: Plot mode shows crosshair, erase shows not-allowed circle, insert shows copy cursor
5. **Snap zoom**: Zoom out → place waypoint on route line → easier to click (200m tolerance)
6. **Numbering**: Add waypoint near end, then near start → numbering reorders correctly (W1 nearest to S)
7. **Edit**: Click an existing POI marker → edit dialog opens pre-filled → save updates the waypoint

---

## Session 2: Fix cursor enforcement + cluster navigation includes all visible routes

### Summary
Two fixes: (1) Tool cursors (crosshair, not-allowed, copy) weren't actually showing because Mapbox's internal event handlers reset the cursor on every mouse interaction. (2) Cluster navigation now expands to include ALL visible routes (both unclustered pins and routes inside visible clusters) as you zoom/pan.

### Files Modified
- `components/routes/routes-map-mapbox.tsx` — Cursor enforcement via persistent `mousemove` handler; `notifyVisibleRoutes` function expands visible clusters via `getClusterLeaves` to gather all route IDs in viewport
- `app/routes/page.tsx` — Restored `handleVisibleRoutesChange` to expand cluster browse with all visible routes (capped at 50)

### What Changed and Why

**Cursor not showing**: The useEffect set the cursor once, but Mapbox's internal `mouseenter`/`mouseleave`/`dragPan` handlers immediately reset it to empty or "pointer" on any mouse movement. Fixed by adding a persistent `mousemove` event listener that continuously re-applies the cursor during creation mode. The listener is cleaned up when the tool mode changes or creation ends.

**Cluster navigation scope**: The previous fix went too far — it locked navigation to only the clicked cluster's routes. The user wants to navigate ALL visible routes including those inside nearby clusters. Restored the `handleVisibleRoutesChange` expansion, and updated the map's `notifyVisibleRoutes` function to expand visible cluster features via `getClusterLeaves`, so the parent receives IDs from both unclustered pins and clustered routes.

### Verification Steps
1. `npm run build` passes
2. **Cursor**: Enter creation mode → plot mode shows crosshair that persists while moving mouse → switch to erase → not-allowed cursor → insert → copy cursor
3. **Cluster nav expand**: Click a cluster → shows "1 of N" → zoom out → counter grows to include routes from other visible clusters → arrows navigate through all

---

## Session 3: Preserve original spine points when editing routes

### Summary
When editing a route, the map was showing dozens of intermediate coordinates from the snapped road geometry instead of the original few spine points the user placed. Fixed by saving the user's original spine waypoints as `spine_points` in the geometry JSONB, and using those when loading for edit.

### Files Modified
- `app/routes/page.tsx` — Save `spine_points` in geometry payload; load from `spine_points` when entering edit mode
- `app/routes/edit/[id]/page.tsx` — Same: prefer `spine_points` over `coordinates` when extracting waypoints
- `app/api/routes/route.ts` — Added `spine_points` to zod validation schema

### What Changed and Why
**Root cause**: When saving a route with snap enabled, `getRouteGeometry()` returns the full road-following polyline with a coordinate for every bend in the road (e.g. 60+ points for a simple route). When loading for edit, every coordinate became a draggable spine point, making the route nearly uneditable.

**Fix**: The save payload now includes `spine_points: waypoints.map(wp => [wp.lng, wp.lat])` alongside the full geometry coordinates. When loading for edit, both `startEditing()` and the standalone edit page check for `geometry.spine_points` first, falling back to `geometry.coordinates` for routes saved before this fix.

### Verification Steps
1. `npm run build` passes
2. **New routes**: Create a route with 5 spine points → save → edit → only 5 spine points shown
3. **Old routes**: Edit a route saved before this fix → still works (falls back to full coordinates)
4. **Route display unchanged**: The actual route line on the map still uses the full snapped geometry

---

## Session 4: Fix circular route reversion + snap drag fallback + edit spine_points

### Summary
Three fixes: (1) Deleting a waypoint in a circular route now reverts to linear if the remaining last point is >50m from the start. (2) Dragging a spine point with snap enabled now falls back to self-snapping to the nearest road when the neighbor-routing snap fails. (3) Edit page now saves `spine_points` so subsequent edits preserve original spine data.

### Files Modified
- `app/routes/page.tsx` — Added `haversineSimple` helper; `removeWaypoint` now checks circular reversion (distance check + min 3 waypoints)
- `app/routes/edit/[id]/page.tsx` — Added `haversineSimple` helper; `removeWaypoint` now checks circular reversion; save handler includes `spine_points` in geometry payload
- `components/routes/routes-map-mapbox.tsx` — Drag handler fallback: when neighbor-routing snap fails (>25m), tries a self-snap via tiny-offset directions call (50m threshold)

### What Changed and Why

**1. Circular route not breaking on point removal**: `removeWaypoint` in both the creation and edit pages simply removed the waypoint without checking whether the route should revert from circular to linear. Now after removal, if the route is circular, it checks: (a) fewer than 3 remaining points → linear, (b) last point >50m from first → linear. On the creation page, also re-enables plotting mode.

**2. Snap drag floating disconnected**: The drag handler snaps by routing from a neighbor to the dragged position. If the snapped endpoint was >25m from the drag position (user dragged off-road), it kept the raw unsnapped position — leaving the point floating disconnected. Now it tries a fallback: a tiny-offset directions call at the dragged position itself, snapping to the nearest road within 50m. This finds the closest road even when the neighbor-routing approach overshoots.

**3. Edit page missing spine_points**: The edit page's save handler sent `geometry.coordinates` but not `geometry.spine_points`. After editing and saving, the spine data was lost — the next edit would show full snapped coordinates instead of the original spine points. Now saves `spine_points` alongside coordinates.

### Verification Steps
1. `npm run build` passes
2. **Circular reversion**: Create/edit a circular route → delete the last point before start → route badge changes to "Linear", plotting re-enabled
3. **Snap drag**: Enable snap → drag a spine point off the road → it snaps to the nearest road instead of floating disconnected
4. **Edit spine preservation**: Edit a route → save → edit again → only original spine points shown (not full road coordinates)

---

## Session 5: Redesign route list cards to match detail card aesthetic

### Summary
Redesigned route cards in Saved Routes and Find Routes panels from a compact horizontal thumbnail+text layout to vertical cards with a map preview header, matching the detail drawer's visual style.

### Files Modified
- `components/routes/saved-routes-panel.tsx` — Replaced horizontal card layout with vertical card: rounded-xl container with shadow, full-width map preview header (h-28) with green gradient fallback, circular badge overlay, title/stats/difficulty in content area
- `components/routes/find-routes-panel.tsx` — Same vertical card redesign plus: bookmark button restyled as a rounded pill overlaid on the map preview (green filled when saved, frosted glass when not), private badge moved inline with title

### What Changed and Why

**Old design**: Small 80x80 square thumbnail on the left, title/stats crammed to the right. Felt like a basic list, didn't match the polished detail card aesthetic.

**New design**: Full-width vertical cards inspired by the route detail drawer:
- **Map preview header** (h-28) using wider Mapbox Static Images (400x200, blue route line) with green gradient fallback when no geometry available
- **Circular badge** overlaid on the map preview (top-left, frosted glass style) when route is circular
- **Bookmark button** (Find Routes only) restyled from a tiny corner cutout to a prominent 32px pill on the map preview — green filled when saved, frosted white when not
- **Content section** with semibold title, stats row using middot separators (distance + estimated ride time), difficulty badge and star rating
- **Ride time fallback**: If `estimated_time_minutes` is missing, calculates from distance at 8 km/h (matching detail drawer)
- **Rounded-xl with shadow-sm → shadow-md on hover** for depth, matching Padoq's Airbnb-inspired design language

### Verification Steps
1. `npm run build` passes
2. **Saved Routes**: Open Saved Routes panel → cards show vertical layout with map preview header, green gradient fallback for routes without geometry
3. **Find Routes**: Open Find Routes panel → same card style with bookmark button on map preview; bookmark fills green when saved
4. **Hover**: Cards lift slightly on hover (shadow-md)
5. **Route type**: Circular routes show a frosted "Circular" badge on the map preview
6. **Click**: Cards still navigate to route detail on click

---

## Session 6: Route list cards — full detail card style (cover photos, stat grid, description)

### Summary
Second iteration of route card redesign. Cards in Saved Routes and Find Routes panels now fully match the detail card aesthetic with cover photos, large stat grids, description previews, and bolder typography.

### Files Modified
- `components/routes/saved-routes-panel.tsx` — Cover photo priority over map thumbnail with green gradient + ImageIcon fallback, bottom gradient overlay, distance pill on image, bold 15px title with star rating, 1-line description preview, 2-column stat grid with large bold numbers (Distance + Est. Ride Time), difficulty badge with review count, rounded-2xl container with hover shadow lift
- `components/routes/find-routes-panel.tsx` — Same redesign plus bookmark button (green when saved), county shown instead of review count

### What Changed and Why

**User feedback**: First redesign (Session 5) was too minimal — still compact with small text. User requested cards that look like "a middle card like our main route card" with the full detail card aesthetic.

**Changes**:
- **Cover photo**: Uses `cover_photo_url` first, then Mapbox Static Image thumbnail, then green gradient + ImageIcon as last fallback
- **Image overlays**: Bottom gradient for text contrast, distance pill badge (bottom-left), circular route badge (top-left), bookmark button on find routes (top-right)
- **Title row**: Bold 15px title with inline star rating and count
- **Description**: 1-line preview with line-clamp-1
- **Stat grid**: 2-column grid with large bold numbers (text-lg) for Distance (km) and Est. Ride Time, separated by border-t divider — matches detail drawer style
- **Footer**: Difficulty badge (color-coded) with review count (saved) or county (find)
- **Removed**: Unused `Clock`, `Ruler` imports replaced by stat grid approach; added `ImageIcon` import

### Verification Steps
1. `npm run build` passes
2. **Cover photos**: Routes with cover photos show the photo; without, shows map thumbnail; without geometry, shows green gradient with ImageIcon
3. **Stat grid**: Large bold numbers for distance and ride time in a 2-column layout
4. **Description**: Shows first line of description below title
5. **Bookmark**: Find Routes cards show bookmark pill on image — green when saved
6. **Hover**: Cards lift with shadow-lg on hover
7. **Responsive**: Cards fill panel width on both desktop and mobile

---

## Session 7: Route panels — centered modal overlay (replacing side panels)

### Summary
Replaced the side panel layout for Find Routes and Saved Routes with centered modal overlays matching the main route detail card's sizing and style. Cards now display in a responsive 2-column grid inside the overlay.

### Files Modified
- `components/routes/saved-routes-panel.tsx` — Complete rewrite: side panel → centered modal overlay (`max-w-2xl max-h-[85vh]`, `bg-black/40 backdrop-blur-sm` backdrop, `rounded-2xl shadow-2xl`), sticky header with title/search/close, scrollable content with sort/tabs/filters/grid, 2-column responsive card grid, removed RoutesPanelHeader/MobileTopHeader/MobilePanelToggle dependencies
- `components/routes/find-routes-panel.tsx` — Same centered modal rewrite plus: collapsible filters toggle (pill button with active count badge), filters in a subtle `bg-gray-50/50` section, removed RoutesPanelHeader/MobileTopHeader/MobilePanelToggle dependencies

### What Changed and Why

**User feedback**: Route panels should not be side panels — they should be centered cards matching the main route detail card's exact sizing, shape, and style.

**Layout change**:
- **Before**: `absolute top-0 left-0 bottom-0 w-96` side panel on desktop, full-screen sliding panel on mobile
- **After**: `fixed inset-0 z-50 flex items-center justify-center p-4` centered overlay with `max-w-2xl max-h-[85vh] md:max-h-[80vh]` — exactly matching the route detail drawer
- Backdrop: `bg-black/40 backdrop-blur-sm` (same as detail card)
- Animation: `animate-in zoom-in-95 slide-in-from-bottom-4 fade-in duration-300` (same as detail card)

**Header simplification**: Replaced the heavy RoutesPanelHeader (hamburger menu, profile avatar, separate search) with a minimal sticky header (title, close button, inline search). The hamburger menu and profile are already accessible via the main route nav tabs.

**Filters (Find Routes)**: Instead of showing all filters at all times (consuming half the viewport), filters are now collapsible behind a "Filters" pill button. The pill shows the count of active filters with a green badge. Expands to show distance slider, difficulty pills, rating pills, route type checkboxes, and "Routes Near Me" button.

**Card grid**: Route cards now display in a responsive `grid-cols-1 md:grid-cols-2` layout with `gap-4`, giving a proper grid browsing experience instead of a narrow single-column list. Cards have a subtle lift effect on hover (`hover:-translate-y-0.5`).

**Removed dependencies**: No longer imports RoutesPanelHeader, MobileTopHeader, or MobilePanelToggle — the centered modal works identically on both desktop and mobile.

### Risk Notes
- The `mobilePanelOpen` and `onMobilePanelToggle` props are still accepted but no longer used (kept for backward compatibility with parent). These can be cleaned up later.
- RoutesPanelHeader, MobileTopHeader, and MobilePanelToggle components are no longer imported but still exist in the codebase. They may be used elsewhere (e.g., waypoint panel) — do not delete them.

### Verification Steps
1. `npm run build` passes
2. **Find Routes**: Click Find Routes tab → centered modal overlay appears over map with backdrop blur
3. **Saved Routes**: Click Saved Routes tab → same centered modal overlay
4. **Card grid**: On desktop, cards display in 2-column grid; on mobile, single column
5. **Close**: Click X button or backdrop to close overlay and return to map
6. **Filters**: Find Routes → click "Filters" pill → filter section expands/collapses
7. **Search**: Type in search bar → routes filter in real time
8. **Card click**: Click any route card → detail card opens (same as before)
9. **Bookmark**: Find Routes → click bookmark button on card → saves/unsaves route

---

## Session 8: Fix waypoint deletion breaking snapped route + widen snap threshold

### Summary
Fixed the route line breaking into a straight line when deleting a middle waypoint with snap enabled. Also widened the snap drift threshold from 15m to 50m so clicks near (but not exactly on) rural bridleways still snap correctly.

### Files Modified
- `components/routes/routes-map-mapbox.tsx` — Erase click handler now re-fetches the connecting segment between remaining adjacent waypoints after deletion (using Directions API, same pattern as drag handler). Snap drift threshold increased from 15m to 50m. Added console.warn logging for all snap failure paths.

### What Changed and Why

**1. Waypoint deletion breaking route line**: When deleting a middle waypoint, `reindexSegmentsOnRemove` correctly cleared the two segments touching the deleted point and reindexed the rest. But it never re-fetched the NEW segment connecting the remaining adjacent waypoints. The route rendering fell back to a straight line for that gap. Fix: the erase handler now fetches `prev→next` segment via Directions API and stores it before triggering the state update.

**2. Snap drift threshold too tight**: The threshold for accepting a snapped point was 15m — if you clicked more than 15m from a path, it silently placed an unsnapped point. In rural areas, this caused confusing behavior. Widened to 50m for both first-waypoint and segment snapping.

**3. Silent snap failures**: All snap failure paths (API error, no route found, drift too large) now log `console.warn` messages so developers can diagnose issues.

### Verification Steps
1. `npm run build` passes
2. **Delete middle waypoint**: Create 3+ snapped waypoints → switch to erase → delete the middle one → route stays on-road (no straight line)
3. **Delete first/last**: Delete first or last waypoint → remaining segments stay snapped
4. **Snap near path**: Click ~30m from a bridleway with snap on → still snaps (was failing at 15m threshold)
5. **Console logging**: Open DevTools → snap failures show "Snap: ..." warnings

---

## Session 9: Fix route detour when placing waypoints close together

### Summary
Fixed the Mapbox Directions API "routing all the way around" problem — when two waypoints are close together, the walking profile sometimes finds a long detour through the road network instead of the short direct path. Added a route distance sanity check that compares the API route distance to the straight-line distance and falls back to a straight line segment when the detour ratio exceeds 5x.

### Files Modified
- `components/routes/routes-map-mapbox.tsx` — Added `isRouteDetour()` helper function; applied detour check in 5 locations: click handler (new waypoint), drag handler (prev→dragged and dragged→next segments), erase handler (prev→next re-fetch), and fetchAllSegments bulk re-snap

### What Changed
1. **New `isRouteDetour()` helper**: Compares Directions API route distance (meters) to haversine straight-line distance. Returns true if route is >5x the straight line (ignoring very short segments <10m).
2. **Click handler**: When placing a new waypoint with snap on, the waypoint still snaps to the nearest road but the segment geometry falls back to straight line if the API route is a detour.
3. **Drag handler**: Both adjacent segment re-fetches (prev→dragged, dragged→next) skip storing snapped geometry if the route is a detour.
4. **Erase handler**: The connecting segment re-fetch after deleting a waypoint skips storing if detour.
5. **fetchAllSegments**: Bulk re-snap (triggered on snap toggle) skips detour segments.

### Verification Steps
1. `npm run build` passes
2. **Close waypoints**: Place two waypoints close together (~50-100m) with snap on → line goes straight between them, not a multi-km detour
3. **Normal spacing**: Place waypoints 500m+ apart → road-following geometry still works as before
4. **Drag near existing**: Drag a waypoint close to another → no detour loop
5. **Console logging**: Detour cases log "Snap: route detour Xx" warnings in DevTools

---

## Session 10: Fix erase+undo race condition and tighten snap drift

### Summary
Fixed two issues: (1) A race condition where the async erase handler could re-remove a waypoint after undo restored it, and (2) the snap drift threshold was too wide at 50m, causing points to jump to adjacent paths.

### Files Modified
- `components/routes/routes-map-mapbox.tsx` — Fixed erase handler ordering, added validity check for async segment fetch, tightened snap drift from 50m to 25m

### What Changed
1. **Erase+undo race condition**: The erase handler called `onWaypointRemove` AFTER an async segment fetch. If the user clicked undo during that fetch, the fetch completed and then fired `removeWaypoint` on the already-restored waypoints — effectively re-erasing what was just undone. Fix: call `onWaypointRemove` immediately before the async fetch, and add a validity check (`stillValid`) that verifies waypoints haven't changed (via undo) before writing the fetched segment.
2. **Snap drift 50m→25m**: The 50m threshold let points jump to adjacent parallel paths/roads. Tightened to 25m which still handles rural clicking inaccuracy but prevents cross-path snapping.

### Verification Steps
1. `npm run build` passes
2. **Erase+undo**: Place 3+ waypoints → erase middle one → immediately click undo → all waypoints restored correctly, F marker on correct point
3. **Rapid erase+undo**: Quickly erase and undo in succession → no double-erasure or stale segments
4. **Snap precision**: Click on a road with a nearby parallel path → point stays on clicked road, doesn't jump
