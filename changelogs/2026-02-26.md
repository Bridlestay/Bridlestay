# Changelog — 2026-02-26

## Session: Review Edit Pre-fill, Compact Review Cards, Map Pin Auto-Refresh

Three changes: review editing now pre-fills existing data, review cards redesigned to a compact expandable format inspired by OS Maps, and map route pins auto-refresh every 10 seconds when on the main map.

---

### 1. Review Edit Pre-Population

**File:** `components/routes/route-detail-drawer.tsx`

When clicking "Update my review", the review flow now pre-fills:
- **Rating** (step 1) — stars pre-selected from existing `completion.rating`
- **Tags** (step 3) — checkboxes pre-ticked from existing `completion.tags`
- **Short note** (step 4) — textarea pre-filled from existing `completion.short_note`
- **Step navigation** — `maxVisitedStep` set to 4 so all steps are clickable, allowing the user to jump directly to whichever part they want to edit or skip past unchanged steps

Previously, clicking "Update my review" started fresh every time, losing all previous review data.

**Fix for stale data:** The completions API now also merges `rating` from `route_reviews` as a fallback (since bare completions from the old `/review` endpoint have no rating). The short note pre-fill falls back to `review_body` when `short_note` is empty (covers old reviews where notes weren't stored in the completion).

### 2. Compact Review Cards (OS Maps Inspired)

**File:** `components/routes/route-detail-drawer.tsx`

Redesigned review cards from large full-detail cards to a compact expandable format:

**Collapsed state (default):**
- Compact single row: avatar (32px), name + date on same line, 2-line text clamp
- Photo teaser strip: small 48x64px thumbnails previewing photos (up to 3 + overflow count)
- "Tap to read more" hint when there's more content (long text, photos, or tags)
- Clean border, no background fill — lightweight and scannable

**Expanded state (tap to open):**
- Full review text with `whitespace-pre-line` for line breaks
- Tags as pill-shaped spans with emoji
- Full photo grid (responsive 1/2/3 columns based on count)
- Photo click opens lightbox (with `e.stopPropagation()` to prevent collapse)
- "Show less" collapse button at bottom

**Other changes:**
- Default visible reviews increased from 3 to 5 (compact cards take less space)
- Section header simplified: "Rider Reviews" with count text
- "Show all" button as subtle outline pill

### 3. Map Pin Auto-Refresh

**File:** `app/routes/page.tsx`

Route pins on the main map now auto-refresh every 10 seconds. This ensures pins appear even if the initial load had a timing issue or if new routes have been published.

**Conditions** — polling only runs when:
- `activeTab === "map"` (user is on the map tab)
- `!isCreating` (not in route creation mode)
- `!selectedRouteId` (no route detail drawer open)

The interval is cleaned up when any condition changes (user selects a route, switches tab, starts creating). This avoids unnecessary API calls in non-map states.

### 4. Completions API Resilience (from prior session, included in this push)

**File:** `app/api/routes/[id]/completions/route.ts`

- Graceful fallback when `review_text` column doesn't exist (tries `review_text` first, falls back to `body`)
- Photos included per completion from both `route_user_photos` and `route_photos` tables
- Each completion response now includes `{ photos: [...] }` array

---

### Files Modified

| File | Changes |
|------|---------|
| `components/routes/route-detail-drawer.tsx` | Review edit pre-fill, compact expandable cards, long note refresh |
| `app/routes/page.tsx` | 15-second pin auto-refresh on main map |
| `app/api/routes/[id]/completions/route.ts` | Resilient review_text query, photos per completion |

### 5. Review Update Actually Saves Tags & Notes (Complete Endpoint Upsert)

**File:** `app/api/routes/[id]/complete/route.ts`

The `/complete` endpoint previously only supported INSERT. When updating a review, it failed with 409 (unique constraint) and the updated tags/short_note were silently lost. The old completion's empty `notes` field was preserved.

**Fix:** Changed to check-then-upsert pattern:
- Queries for existing completion by `user_id + route_id`
- If found: UPDATE the existing row's `notes` and `rating` fields (preserves `completed_at`)
- If not found: INSERT a new row as before

This means editing a review now correctly saves updated tags and short notes to the completion.

### 6. Long Note Pre-Population & Preservation

**File:** `components/routes/route-detail-drawer.tsx`

When editing a review that previously had a long note (step 5):
- The long note is extracted from `review_body` by splitting on the `\n\n---\n\n` separator
- `reviewLongNote` is pre-populated so step 5 shows the existing extended text
- `maxVisitedStep` set to 5 (was 4) so step 5 is navigable during edits
- `handleSubmitRideReview` now includes the long note in `review_text` if present, preventing the update from overwriting combined text with just the short note

### 7. Clickable Reviewer Profiles

**File:** `components/routes/route-detail-drawer.tsx`

Reviewer avatars and names in the compact review cards are now clickable links to `/profile/[userId]`. The avatar shows a green ring on hover as a visual affordance. Uses `e.stopPropagation()` to prevent the click from toggling the card expand/collapse.

### 8. Review Card Display Fixes

**File:** `components/routes/route-detail-drawer.tsx`

- **Duplicate text eliminated**: Collapsed text is now hidden when the card is expanded, so the review text only appears once in the expanded detail area
- **API error checking added**: Both `/complete` and `/review` API calls in `handleSubmitRideReview` now check response status and log errors. `handleSubmitLongNote` now shows a toast on failure instead of silently swallowing errors
- **Step 5 button clarity**: "Skip" button now shows "Done" when there's no long note text, and "Cancel" when text is present (alongside "Save Note")

### 9. Long Note Storage Moved to Completions (Critical Fix)

**Files:** `components/routes/route-detail-drawer.tsx`, `app/api/routes/[id]/completions/route.ts`

The "Share more with other riders" long note from step 5 was being saved to `route_reviews.review_text` via the `/review` endpoint, which was unreliable (RLS policies, column existence issues). **Long notes never persisted.**

**Fix:** Long notes are now stored in `route_completions.notes` JSON alongside tags and short_note:
```json
{ "tags": ["road-crossings"], "short_note": "Bring Sunscreen", "long_note": "Detailed ride description..." }
```

- `handleSubmitRideReview` includes `long_note` in the notes JSON sent to `/complete`
- `handleSubmitLongNote` now calls `/complete` (proven upsert) instead of `/review`
- Completions API parses `long_note` from the JSON notes field
- Review card shows `short_note` as main text, `long_note` in a left-bordered block
- Pre-populate reads `long_note` directly from the completion object

### 10. Photo Management During Review Edit

**File:** `components/routes/route-detail-drawer.tsx`

- **Delete button**: Each existing photo on step 2 now has an X button (visible on hover) that calls the DELETE endpoint to remove the photo
- **Refresh after upload**: After uploading a new photo in step 2, completions are refreshed so the new photo immediately appears in the existing photos list
- **Photo count**: Shows "Your photos (N)" label above existing photos

---

### Files Modified

| File | Changes |
|------|---------|
| `components/routes/route-detail-drawer.tsx` | Long note via /complete, photo deletion, photo refresh, display fixes, error checking |
| `app/routes/page.tsx` | 10-second pin auto-refresh on main map |
| `app/api/routes/[id]/completions/route.ts` | Parse and return `long_note` from notes JSON |
| `app/api/routes/[id]/complete/route.ts` | Changed from insert-only to upsert |

### 11. Photo Upload Fix — Wrong Column Name (`created_at` vs `uploaded_at`)

**Files:** `app/api/routes/[id]/completions/route.ts`, `app/api/routes/[id]/photos/route.ts`

**Root cause:** Every SELECT query against `route_user_photos` referenced a `created_at` column that doesn't exist. The actual column (from migration 030) is `uploaded_at`. PostgREST returns `{ data: null }` for queries with invalid columns, and the error was silently swallowed by `catch` blocks or `data || []` fallbacks. This meant **all photos in `route_user_photos` were invisible** — uploads succeeded but reads always returned empty arrays.

**Fix:** Changed `created_at` → `uploaded_at` in:
- `completions/route.ts` — `.select("id, user_id, url, caption, uploaded_at")` and `.order("uploaded_at", ...)`
- `photos/route.ts` GET handler — `.order("uploaded_at", ...)` for the `route_user_photos` query

**Additional (from prior push):** POST handler now uses `createServiceClient()` for storage upload and DB inserts to bypass RLS. `fetchCompletions()` is now `await`ed after upload.

---

### Risk Notes

- Auto-refresh makes 2 API calls (search + my-routes) every 10 seconds. Both are simple Supabase queries. For heavy traffic this could be optimised with cache headers or conditional fetching, but for current scale this is fine.
- The `/complete` endpoint now does a SELECT + UPDATE/INSERT instead of a single INSERT. This adds one extra query but is necessary for correct update behavior.
- Compact card photo teasers use small fixed-size thumbnails (48x64px). These load full images scaled down — could be optimised with image transforms in the future.
- Photo deletion uses the existing DELETE endpoint which only allows users to delete their own photos from `route_user_photos`.
- Photo upload and deletion both use service role client to bypass RLS. Auth is verified separately before any write operation.

### Verification

1. Submit a review with tags + short note → compact card appears
2. On step 5, type a long note → click "Save Note" → toast confirms
3. View the review card → short text shown, long note shown in bordered block below
4. Click "Update my review" → step 5 shows the previously saved long note
5. Navigate to step 2 → existing review photos are shown with delete buttons
6. Hover a photo → X button appears → click → photo removed
7. Upload a new photo → it appears in existing photos after completing the step
8. Tap a review card → expands to show text, tags, photos (text appears ONCE)
9. Click a reviewer's avatar or name → navigates to `/profile/[userId]`
10. If API call fails → error toast appears (check browser console for details)
11. Open the main map → pins refresh every 10 seconds

---

## Session: Weather Conditions for Route Detail View

Added live weather data to the route detail drawer using the free Open-Meteo API. When viewing a route, riders now see current conditions, a 3-day forecast, and extreme weather alerts.

### 12. Weather Utility Module

**File:** `lib/weather.ts` (NEW)

Shared types and utilities for weather data:
- `WeatherData`, `WeatherCurrent`, `WeatherDay` TypeScript interfaces
- WMO weather code mapping (0-99) → emoji icons, condition text, warning/severe flags
- `getRouteCentroid()` — averages all [lng, lat] pairs from route geometry to get a center point
- `roundCoords()` — rounds to 2 decimal places (~1.1km) for cache keys so nearby routes share weather data
- `buildWarningMessage()` — generates rider-friendly warning text including wind gusts >60 km/h (horses may spook)

### 13. Weather API Endpoint

**File:** `app/api/weather/route.ts` (NEW)

Server-side GET endpoint accepting `?lat=...&lng=...` query params:
- Validates coordinates are within UK bounds (lat 49-61, lng -11 to 3)
- Rounds coordinates for cache key, checks server-side in-memory cache (30-min TTL)
- On cache miss: fetches current + 3-day forecast from Open-Meteo (`api.open-meteo.com`)
- Parses response into structured `WeatherData` with current conditions, daily forecast, warning flags
- Cache capped at 500 entries with automatic cleanup to prevent memory leaks
- Returns 502 if Open-Meteo is unavailable (graceful degradation)

### 14. Weather Section in Route Detail Drawer

**File:** `components/routes/route-detail-drawer.tsx`

New weather section between route map snapshot and "I've ridden this route" button:
- **Loading skeleton** while fetching weather data
- **Section header** with current weather emoji icon + "Weather Conditions"
- **Warning banner** (conditional): amber for warnings, red for severe conditions. Shows `AlertTriangle` icon + rider-friendly message
- **Current conditions grid** (4 columns, same style as stats grid): temperature, condition icon, wind speed, precipitation
- **3-day forecast** cards: day name, weather icon, high/low temperature. Cards turn amber/red for warning/severe days
- Weather fetched via `cache.getOrFetch()` with 30-min client-side TTL
- Silently fails if weather unavailable — section simply doesn't appear

### 15. Cache Update

**File:** `lib/cache.ts`

Added `weather: 30 * 60 * 1000` (30 minutes) to `CACHE_TTL` object.

---

### Files Created

| File | Purpose |
|------|---------|
| `lib/weather.ts` | Types, WMO code mappings, centroid calculation, warning builder |
| `app/api/weather/route.ts` | Server-side weather API endpoint with caching |

### Files Modified

| File | Changes |
|------|---------|
| `components/routes/route-detail-drawer.tsx` | Weather state, fetch effect, weather section UI |
| `lib/cache.ts` | Added `weather` TTL constant (30 min) |

### Risk Notes

- Open-Meteo is a free service with no SLA. If it goes down, weather section silently disappears (graceful degradation).
- Server-side cache is per serverless instance, not shared across Vercel functions. Worst case, a cold function re-fetches.
- No new npm packages, no database changes, no new environment variables.

### Verification

1. `npm run build` — no errors
2. Open a route detail drawer — weather section appears between map snapshot and action button
3. Shows current temp, conditions, wind, precipitation in a 4-column grid
4. Shows 3-day forecast cards below
5. If weather has warnings — amber/red banner appears with rider-friendly message
6. Open a second route in the same area — weather loads from cache
7. Route with no geometry — no weather section (graceful)
