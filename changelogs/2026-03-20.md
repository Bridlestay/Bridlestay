# Changelog — 2026-03-20

## Session 1

### Summary
Redesigned the "Ridden" button to remove ride count and visible countdown timer. Button now shows a subtle grey pill when on cooldown with a hover tooltip showing remaining time.

### Changes

#### Ridden button redesign
- **File modified**: `components/routes/route-detail-drawer.tsx`
- **What changed**:
  - Button always shows "Ridden" + checkmark after first ride (no more "Ridden 3x" count)
  - Removed visible countdown timer text from button label (was showing "2h 14m 30s")
  - Cooldown state now shows a subtle grey pill (`bg-gray-100 text-gray-400`) instead of ghost with Clock icon
  - Added Radix Tooltip on hover during cooldown: "Can't ride again for Xh Xm"
  - Removed unused `Clock` import from lucide-react
  - Added `Tooltip`, `TooltipContent`, `TooltipProvider`, `TooltipTrigger` imports

### Files Modified
- `components/routes/route-detail-drawer.tsx` — Ridden button UI

### Verification Steps
1. Open a route detail drawer
2. Click "Ridden" button (green, first time)
3. After clicking, button should show grey pill with "Ridden" + checkmark — no count, no timer
4. Hover over greyed button — tooltip shows "Can't ride again for Xh Xm"
5. After cooldown expires, button returns to green outline "Ridden" + checkmark (clickable again)

---

## Session 2

### Summary
Multiple UX improvements: larger route pin tap targets, better route zoom/fitBounds, photo reminder toast during recording, variant "Copy from original" description button, and missing Star icon import fix.

### Changes

#### Route pin click targets enlarged
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- Hit detection circle increased from 20px to 30px radius
- Pin SVG enlarged from 32x42 to 36x47 with 8px padding for easier finger taps on mobile

#### Route zoom/fitBounds improved
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- Mobile padding: `{ top: 120, bottom: 200, left: 60, right: 60 }` (was 100/150/40/40)
- Desktop padding: `{ top: 100, bottom: 100, left: 480, right: 100 }` (was 80/80/450/80)
- maxZoom increased from 14 to 15 for short routes
- **File modified**: `app/routes/page.tsx`
- Removed two hardcoded tight padding overrides that were squeezing routes on mobile — now uses the improved defaults

#### Photo reminder toast during recording
- **File modified**: `components/routes/route-recorder.tsx`
- Toast fires 2 minutes into GPS recording: "Don't forget to take photos along the way!"
- "Don't remind me" action button persists preference to `localStorage` (`padoq_hide_photo_reminder`)
- Respects pause state — timer resets if paused

#### Variant "Copy from original" description button
- **File modified**: `components/routes/save-route-modal.tsx`
- New prop: `variantOfRouteId` — parent route ID
- "Copy from original" button appears next to Description label when creating a variant
- Fetches parent route description via API and fills it in
- Loading spinner while fetching
- **File modified**: `app/routes/page.tsx`
- Passes `variantOfRouteId` prop to `SaveRouteModal`

#### Missing Star icon import fix
- **File modified**: `components/routes/find-routes-panel.tsx`
- Added `Star` to lucide-react imports — was used in rating filter buttons but never imported (would cause runtime crash)

### Files Modified
- `components/routes/routes-map-mapbox.tsx` — Pin size + fitBounds padding
- `app/routes/page.tsx` — fitBounds padding overrides removed, variant prop passed
- `components/routes/route-recorder.tsx` — Photo reminder toast
- `components/routes/save-route-modal.tsx` — Copy description button
- `components/routes/find-routes-panel.tsx` — Star import fix

### Verification Steps
1. Tap route pins on mobile — should be noticeably easier to hit
2. Click any route — should zoom to fit nicely centered with breathing room on both mobile and desktop
3. Start recording a route — after 2 minutes a toast should appear reminding to take photos. Click "Don't remind me" and it shouldn't appear again
4. Create a variant route — save modal should show "Copy from original" button next to Description
5. Open Find Routes → Filters → Rating filter buttons should render without errors (Star icon)

---

## Session 3

### Summary
Fixed route zoom so the route is always centered and fully visible above the mini card overlay. Increased bottom padding from 100px to 220px (desktop) and 200px to 250px (mobile) to ensure the route line never hides behind the mini card.

### Changes

#### Route zoom centering fix
- **File modified**: `components/routes/routes-map-mapbox.tsx`
- **What changed**: Reworked `fitBounds` default padding to account for the ~170px mini card at the bottom of the screen
  - Mobile: `{ top: 100, bottom: 250, left: 50, right: 50 }` — extra bottom padding pushes route above mini card
  - Desktop: `{ top: 80, bottom: 220, left: 100, right: 100 }` — same principle, left padding reduced since most views don't have a side panel during route preview
- **Why**: When clicking a route pin, the route line was partially hidden behind the mini card overlay. The fitBounds was centering on the full viewport without accounting for the card.

### Files Modified
- `components/routes/routes-map-mapbox.tsx` — fitBounds padding

### Verification Steps
1. Click a route pin on desktop — entire route should be visible above the mini card, nicely centered
2. Click a route pin on mobile — same, route fully visible above the card
3. Short routes (< 1km) and long routes (> 10km) should both fit well
4. Close the mini card — route stays drawn on map at the same zoom level

---

## Session 4

### Summary
Added tooltips to map control buttons (locate, layers, zoom), fixed zoom button stretching, and fixed locate-me for desktop browsers.

### Changes

#### Map control tooltips
- **File modified**: `components/routes/map-layer-controls.tsx`
- Added Radix Tooltip to all map control buttons (side="left"):
  - Locate: "Find my location" / "Re-centre on location" (dynamic based on state)
  - Layers: "Map settings"
  - Zoom in/out: "Zoom in" / "Zoom out"
- Style matches the route creation toolbar tooltips

#### Zoom button stretching fix
- **File modified**: `components/routes/map-layer-controls.tsx`
- Added explicit `w-11` to the zoom button container div
- Changed zoom button classes from `h-10 w-10` to `h-10 w-11` to match container width
- Ensures symmetrical pill shape with no stretching

#### Locate-me desktop fix
- **File modified**: `app/routes/page.tsx`
- Root cause: stale closure — `userPosition` was captured as `null` in the `watchPosition` callback and never updated, so the "first fix" centering was unreliable
- Fix: replaced with a ref (`locateCenteredRef`) to track whether we've centered
- Added `getCurrentPosition` one-shot call before `watchPosition` — desktop browsers resolve WiFi/IP geolocation faster with one-shot than continuous watching
- Cleaner error handling: consolidated duplicate retry logic into a `startWatch` helper

### Files Modified
- `components/routes/map-layer-controls.tsx` — Tooltips + zoom button width
- `app/routes/page.tsx` — Locate-me desktop fix

### Verification Steps
1. Hover over locate button → tooltip "Find my location" appears to the left
2. Click locate → tooltip changes to "Re-centre on location"
3. Hover over layers button → "Map settings"
4. Hover over +/- buttons → "Zoom in" / "Zoom out"
5. Zoom buttons should be perfectly round pill shape, not stretched
6. On desktop: click locate-me → should get a position fix and center the map (may take a few seconds for WiFi geolocation)
