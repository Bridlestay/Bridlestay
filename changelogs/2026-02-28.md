# Changelog — 2026-02-28

## Session: Spring Cleaning — Custom Commands, QA Rules, Code Review Fixes

Set up developer tooling, added mandatory quality checks to CLAUDE.md, and fixed issues found during code review.

---

### 1. Custom Slash Commands

**Files created:**
- `.claude/commands/discuss.md` — `/discuss` command for strategic planning and architecture discussions. Provides a structured framework: context, current state, options, tradeoffs, recommendation, open questions.
- `.claude/commands/uxamine.md` — `/uxamine` command for deep UI/UX examination. Includes a full checklist covering visual design, interaction design, information architecture, responsiveness, and accessibility. Outputs structured findings with severity ratings.

**Fix:** Commands initially failed with "Unknown skill" error because they were missing YAML frontmatter. Added `---` frontmatter blocks with `name`, `description`, and `argument-hint` fields to both files. Commands now register correctly (requires session restart to pick up).

### 2. CLAUDE.md Quality Rules

**File modified:** `CLAUDE.md`

Added two new mandatory sections:

**Post-Change Quality Checks** — after every implementation:
- Diff review for bugs, debug code, security issues
- Functionality preservation check
- Documentation and design alignment
- Test alignment verification
- Task completion cross-check

**Post-Change Workflow** — after completing work:
1. Build check (`npm run build`)
2. Full summary of changes
3. Changelog update with correct date
4. Git commit with conventional commit style and push
5. Report concerns/recommendations

Also added **Custom Commands** reference table.

### 3. Code Review Fixes

Issues found during systematic review of recent changes:

**Fix: Photos route storage client (security hardening)**
**File:** `app/api/routes/[id]/photos/route.ts`

Reverted storage upload from service client back to regular authenticated client. The storage bucket policy already allows authenticated uploads (`auth.role() = 'authenticated'`). The service client should only be used for database inserts where RLS blocks the operation. This follows the principle of least privilege — storage operations use the user's auth context, only DB operations bypass RLS.

**Fix: Normalize photo timestamps in completions API**
**File:** `app/api/routes/[id]/completions/route.ts`

Photos from `route_user_photos` have an `uploaded_at` column while `route_photos` has `created_at`. When merging photos from both tables, the timestamp field names were inconsistent. Fixed by mapping `uploaded_at` → `created_at` on user photos so all photos have a uniform `created_at` field. Also added time-based sorting so photos from both sources are properly ordered (newest first).

---

### Files Created

| File | Purpose |
|------|---------|
| `.claude/commands/discuss.md` | `/discuss` slash command definition |
| `.claude/commands/uxamine.md` | `/uxamine` slash command definition |

### Files Modified

| File | Changes |
|------|---------|
| `CLAUDE.md` | Added Post-Change Quality Checks, Post-Change Workflow, Custom Commands sections |
| `app/api/routes/[id]/photos/route.ts` | Reverted storage upload to regular client (service client for DB only) |
| `app/api/routes/[id]/completions/route.ts` | Normalized photo timestamps, added sort |

### Test Impact

- No tests were modified or broken
- Existing tests (4 active Playwright E2E tests) remain valid
- Test coverage was not reduced — recent features (weather, photo upload, reviews) have no test coverage (pre-existing gap)

### Verification

1. `npm run build` — passes
2. Photo upload still works (regular client has authenticated upload policy)
3. Photo deletion still works (service client for DB operations)
4. Completions API returns photos with consistent `created_at` field, sorted newest first
5. `/discuss` and `/uxamine` commands are available in Claude Code sessions

---

## Recent Context (for next session continuity)

This section summarises the broader state of the project so that the next Claude session can pick up where this one left off.

### What was done in the 2026-02-26 session

1. **Weather conditions added to route detail drawer** (`lib/weather.ts`, `app/api/weather/route.ts`, `components/routes/route-detail-drawer.tsx`, `lib/cache.ts`)
   - Uses free Open-Meteo API (no key needed). Server-side endpoint at `/api/weather?lat=...&lng=...` with 30-min in-memory cache.
   - Route detail drawer shows: current conditions (temp, icon, wind, precipitation) in a 4-column grid, 3-day forecast cards, and amber/red warning banners for dangerous conditions (freezing rain, snow, thunderstorms, wind gusts >60km/h — horses spook).
   - Centroid calculated from route geometry to determine weather location.

2. **Photo upload bug fixed** — Root cause: `route_user_photos` table has `uploaded_at` column (from migration 030), but all queries used `created_at`. PostgREST silently returned null for the invalid column, and `data || []` fallback hid the failure. Fixed in `completions/route.ts` and `photos/route.ts`.

3. **Rider reviews system** — Previously built across multiple sessions. Reviews with star ratings, text notes (stored in `route_completions.long_note`), photo grids with lightbox, edit/delete functionality. Stars/ratings are NOT shown publicly — they're for internal use only.

### Key technical notes for the next session

- **Two photo tables**: `route_user_photos` (community/review photos, column: `uploaded_at`) and `route_photos` (owner/admin photos, column: `created_at`). Different timestamp column names — always check which table you're querying.
- **PostgREST silent failures**: Querying a non-existent column returns `{ data: null, error: {...} }`, not a thrown exception. Combined with `data || []` fallback, this silently hides all results. Be careful with column names.
- **Service client usage**: `createServiceClient()` (from `lib/supabase/service.ts`) bypasses RLS. Use ONLY for DB operations where RLS blocks (e.g., cross-user queries). Storage operations should use the regular authenticated client — the bucket policy allows `auth.role() = 'authenticated'`.
- **Route geometry**: Stored as GeoJSON LineString `{ type: "LineString", coordinates: [[lng, lat], ...] }`. The `route` object may have it as `geometry` or `route_geometry` depending on context.

### What's next

**Route main card and detail drawer redesign.** The user and their partner had a meeting and decided they don't like the current route card design. They have a completely redesigned version. The user will share new designs/mockups at the start of the next session. Use `/discuss` to plan the approach and `/uxamine` to audit the result.

Key files that will likely be modified:
- `components/routes/route-detail-drawer.tsx` — the main route detail view (~2700 lines)
- Route card components in `components/routes/`
- Possibly route list/grid layout components

The weather section was just added to the drawer — it should be preserved or adapted to fit the new design.

---

## Session 2: Route Detail Drawer Decomposition

### Summary
Decomposed the monolithic `route-detail-drawer.tsx` (3,712 lines) into 9 focused files totalling 3,310 lines. This is a pure refactor with zero UI or behaviour changes — every feature works exactly as before. Also deleted the accidental `nul` file artifact and added it to `.gitignore`.

### Files Created
| File | Lines | Purpose |
|------|-------|---------|
| `components/routes/route-detail-constants.ts` | 96 | Shared constants: `HAZARD_TYPES`, `WARNING_TYPES`, `REVIEW_TAGS`, `PHOTO_CATEGORIES`, `DIFFICULTY_CONFIG`, `SEVERITY_COLORS`, helper functions (`getDifficultyInfo`, `getTimeRemaining`, `getDistanceMeters`) |
| `components/routes/route-photo-lightbox.tsx` | 88 | Reusable full-screen photo lightbox with swipe navigation, used by both the main carousel and review photos |
| `components/routes/route-weather-section.tsx` | 119 | Weather display: current conditions grid, warning banner, 3-day forecast with skeleton loading |
| `components/routes/route-discussion-panel.tsx` | 130 | Full discussion panel: comment list, posting new comments, reporting |
| `components/routes/route-waypoints-panel.tsx` | 229 | Waypoints accordion panel: tag filtering, OS Grid Refs, elevation, distance calculations, fly-to-location |
| `components/routes/route-hazard-dialogs.tsx` | 593 | Four dialog components: `HazardReportDialog`, `WarningPostDialog`, `ReportCommentDialog`, `DeleteHazardDialog` |
| `components/routes/route-review-flow.tsx` | 646 | 5-step review wizard: star rating, photo upload, tags, short note, warning check/long note |
| `components/routes/route-review-cards.tsx` | 267 | Expandable review cards with photos, tags, and lightbox integration |

### Files Modified
| File | Change |
|------|--------|
| `components/routes/route-detail-drawer.tsx` | Reduced from 3,712 to 1,142 lines. Now serves as orchestrator: state management, data fetching, handlers, and layout composition. Removed dead code (`PhotoCard`, unused loading states, unused dialog state, `refreshPhotos`, `handleResolveHazard`). Removed unused `mobileShowDetails`/`onMobileToggleDetails` props. |
| `app/routes/page.tsx` | Removed `mobileShowDetails` and `onMobileToggleDetails` props from drawer usage (were already dead props) |
| `.gitignore` | Added `nul` to prevent Windows artifact from being tracked |

### Files Deleted
| File | Reason |
|------|--------|
| `nul` | Accidental 0-byte Windows artifact in repo root |

### Architecture Decisions
- **Props/callbacks pattern** for cross-component communication (not React Context) — keeps components testable and self-contained
- **Sub-components manage their own UI state** (expanded items, lightbox, tag filters) while the orchestrator owns data and API state
- **Orchestrator pattern**: `route-detail-drawer.tsx` remains the single entry point, handling all data fetching, auth checks, and passing data down via props
- **Bug fix**: Added `initialExpandedId` prop to `RouteWaypointsPanel` so waypoint auto-expansion works when navigating from a map marker

### Risk Notes
- Pure refactor — no database, API, or UI changes
- All cross-component interactions preserved via props/callbacks
- Build passes cleanly with no errors

### What's Next
- Route card and detail drawer UI redesign (user has new designs/mockups)
- Test coverage improvements (deferred until after redesign)

---

## Session 3: Route Detail Drawer — Phase 1 Core Layout Redesign

### Summary
Redesigned the route detail drawer layout inspired by Komoot. Phase 1 covers the core structural changes: photo hero with overlay buttons, author/meta section with reverse geocoding, reordered content, and a new bottom bar. This is the first of 6 planned phases for the complete redesign.

### Changes

**Photo hero (full-bleed)**
- Taller hero image (h-64 / h-72 on desktop vs old h-52)
- Always renders — shows green gradient placeholder when no photos
- Like button (heart) moved from bottom bar to photo overlay (bottom-right, red when liked)
- Likes count displayed next to heart on photo
- Terrain tags overlaid on photo (bottom-left, frosted glass pills)
- Bottom gradient for text readability over photos
- Photo count badge moved to bottom-left
- Prev/next arrows and dot indicators preserved

**Author + meta section**
- Bigger avatar (h-10 vs h-8), PFP on the left (Komoot-style)
- Two-line layout: name + meta row (date + location)
- Date shown using `updated_at` or `created_at` with `formatDistanceToNow`
- Location name via reverse geocoding (Mapbox Geocoding API, computed from route centroid)

**Content reorder**
- Description now appears before stats (was after)
- Terrain tags conditional: shown on photo when photos exist, shown as badges in content when no photos

**Bottom bar redesign**
- Removed: Like button (moved to photo overlay), Share button (moved to secondary actions)
- Added: Save/Saved button (primary action) + Navigate button (disabled placeholder for future)
- Bottom bar conditionally hidden during review flow, discussion panel, and waypoints panel

**Modal structure**
- Removed: Green gradient header bar with title
- Added: Floating close button (X) — dark frosted glass circle, always visible over scroll content (z-20)
- Scrollable content now starts at the photo hero (no header taking space)

**Secondary actions row**
- Added Share button to the existing GPX / Edit / Report action bar

**Removed**
- Engagement stats grid (likes/completions/shares) — likes now on photo, completions as "times ridden" in meta, shares not needed standalone
- `PHOTO_CATEGORIES` import — no longer used after removing caption category label from photo carousel

### Files Modified

| File | Changes |
|------|---------|
| `components/routes/route-detail-drawer.tsx` | Photo hero, author+meta, content reorder, bottom bar, modal structure, reverse geocoding |

### Architecture Decisions
- **Reverse geocoding on client**: Uses `NEXT_PUBLIC_MAPBOX_TOKEN` directly from client to call Mapbox Geocoding API. One request per drawer open, no caching (route centroid is stable per route). Uses `types=place,locality` and takes first two parts of `place_name` for readable location (e.g., "Dartmoor, Devon").
- **Conditional bottom bar**: Hides during sub-panels and review flow so those views get full vertical space.
- **Floating close button**: Positioned absolutely in the modal card with z-20. Works over both photo hero and white content. Sub-panels have their own back buttons; the X is an escape hatch.

### Risk Notes
- `route.updated_at` is accessed but TypeScript types are stale (field exists in DB per migration 019, auto-update trigger active). Works because `route` is typed as `any`.
- Reverse geocoding adds one Mapbox API call per drawer open — negligible cost, no rate limit concern.
- Navigate button is a disabled placeholder — will be wired up in a future phase.

### Verification
1. `npm run build` — passes
2. Photo hero renders with overlay buttons and terrain tags
3. Author section shows date and reverse-geocoded location
4. Description appears before stats
5. Bottom bar shows Save + Navigate
6. Floating X close button works over photo and content
7. Share moved to secondary actions row
8. All sub-panels (discussion, waypoints, review flow) still functional
9. Like/unlike from photo overlay preserves optimistic update behaviour

### What's Next — Remaining Phases
- **Phase 2**: Photo system (collective pool, collage takeover, cover management)
- **Phase 3**: Comments + social (inline preview, full takeover, reporting)
- **Phase 4**: Review flow simplification (Star → Photos → Tags → Warnings → Thank you, no text)
- **Phase 5**: Tabbed section (Map[mobile] / Elevation / Waypoints / Hazards / Warnings / Weather)
- **Phase 6**: Waypoint contributions (users add photos + descriptions to waypoints)

---

## Session 4: Route Detail Drawer — Phase 2 Photo System

### Summary
Added a collective photo gallery takeover showing all photos (owner + community) in a unified grid. Accessible via the "See all N" badge on the photo hero. Owners can set cover photos from the gallery. Photo lightbox works from both hero carousel and gallery contexts.

### Changes

**Photo gallery takeover (`route-photo-gallery.tsx`)**
- Full-screen gallery panel matching the discussion/waypoints panel pattern
- Header with back button and photo count badge
- 2-column (3 on desktop) grid of square thumbnails
- Cover badge (green, star icon) on the current cover photo
- Community badge (blue, users icon) on community-uploaded photos
- "Set as cover" hover button for owners (only on owner photos)
- Photo info: caption, uploader avatar + name, relative timestamp
- Empty state with prompt to add photos via reviews
- Click any thumbnail to open the lightbox at that index

**Drawer orchestrator changes (`route-detail-drawer.tsx`)**
- Added `userPhotos` state — captures community photos from the existing API response (was returned but ignored)
- Expanded `activeFullPanel` type to include `"photos"`
- Added `allPhotosForGallery` useMemo — normalizes both `route_photos` (owner, `created_at`) and `route_user_photos` (community, `uploaded_at`) into a unified sorted array
- Added `totalPhotoCount` derived value (owner + community)
- Added `handleSetCoverPhoto` handler — calls existing PATCH endpoint, re-fetches all photos on success
- Photo count badge changed from static `"1/3"` display to clickable `"See all N"` button that opens gallery
- Added `"photos"` branch in render conditional (after waypoints, before main content)
- PhotoLightbox now uses `allPhotosForGallery` when gallery is active, `displayPhotosForCarousel` otherwise

### Files Created

| File | Purpose |
|------|---------|
| `components/routes/route-photo-gallery.tsx` | Photo gallery takeover component |

### Files Modified

| File | Changes |
|------|---------|
| `components/routes/route-detail-drawer.tsx` | userPhotos state, gallery memo, cover handler, clickable badge, photos panel branch, lightbox source switch |

### Architecture Decisions
- **Photo normalization**: Both photo tables have different column names (`created_at` vs `uploaded_at`, `uploaded_by_user_id` vs `user_id`, `uploader` vs `user` joins). The `allPhotosForGallery` memo normalizes these into a single `GalleryPhoto` interface sorted by date.
- **Cover management restricted to owner photos**: Only `route_photos` items can be set as cover (not community photos), enforced in UI via `canSetCover` check.
- **Lightbox context switching**: The single `PhotoLightbox` instance switches its photo source based on `activeFullPanel === "photos"` — no duplicate lightbox needed.
- **No API or DB changes**: The GET endpoint already returned `userPhotos` and the PATCH endpoint already handles cover management.

### Risk Notes
- Pure additive — no existing behaviour changed
- Community photos may have null `url` if uploaded before URL storage was added (unlikely but `Image` will show broken state)
- Cover photo PATCH requires owner/admin auth (enforced server-side)

### Verification
1. `npm run build` — passes
2. Photo count badge shows "See all N" with total count and is clickable
3. Clicking badge opens gallery takeover with all photos in grid
4. Cover badge shown on cover photo, community badge on user photos
5. Owner sees "Set as cover" on hover for owner photos
6. Clicking gallery thumbnail opens lightbox at correct index
7. Back button returns to main drawer content
8. Hero carousel still works (click photo = lightbox)
9. Bottom bar hides during gallery (existing `activeFullPanel === null` check)

### What's Next — Remaining Phases
- **Phase 3**: Comments + social (inline preview, full takeover, reporting, no likes, no reply UI)
- **Phase 4**: Review flow simplification (Star → Photos → Tags → Warnings → Thank you, no text)
- **Phase 5**: Tabbed section (Map[mobile] / Elevation / Waypoints / Hazards / Warnings / Weather)
- **Phase 6**: Waypoint contributions (users add photos + descriptions to waypoints)

---

## Session 5: Route Detail Drawer — Phase 3 Comments + Social Redesign

### Summary
Redesigned the discussion panel and inline comment preview. Fixed two bugs where the frontend used `content` but the API returns `body`. Added per-comment actions (report and delete) with a three-dot dropdown menu. Moved the comment input to the bottom of the panel (messaging-app style). Improved the inline preview to show 2-3 recent comments in Instagram-style layout.

### Bug Fixes
- **Comment posting broken**: `route-discussion-panel.tsx` line 67 sent `{ content: newComment }` but the API expects `{ body: string }`. Comments were being created with empty body text.
- **Comment display broken**: `route-discussion-panel.tsx` line 114 displayed `comment.content` but the API returns `comment.body`. Comment text appeared blank.
- **Inline preview display broken**: `route-detail-drawer.tsx` line 1091 displayed `comments[0]?.content` instead of `comments[0]?.body`.

### Changes

**Discussion panel rewrite (`route-discussion-panel.tsx`)**
- Layout flipped: comment input now at bottom (sticky), comments scroll above
- Replaced Textarea with compact Input + Send icon button
- Per-comment three-dot dropdown menu (MoreHorizontal icon):
  - Report: opens ReportCommentDialog with the specific commentId
  - Delete: soft-deletes via existing DELETE endpoint (own comment or admin only, red text)
- Three-dot menu hidden until hover (desktop) but always tappable (mobile)
- Added `userId` and `isAdmin` props for permission checks
- Added `reportingCommentId` and `deletingCommentId` state
- Imported ReportCommentDialog from route-hazard-dialogs (was only used route-level before)
- Replaced ScrollArea with native overflow-y-auto div (avoids DropdownMenu portal clipping)
- Auto-scrolls to top after posting (newest comments appear first)

**Inline discussion preview rewrite (`route-detail-drawer.tsx`)**
- Shows up to 3 most recent comments (was 1)
- Instagram-style: name inline with truncated body ("John Smith This route was amazing...")
- Smaller avatars (h-6 vs h-8) for compact display
- "View all N comments" link when >3 comments
- Separated header (clickable) from comment previews
- Fixed `comment.body` display (was `comment.content`)

**Props wiring (`route-detail-drawer.tsx`)**
- Passes `userId` and `isAdmin` to RouteDiscussionPanel (both already existed as state)

### Files Modified

| File | Changes |
|------|---------|
| `components/routes/route-discussion-panel.tsx` | Full rewrite: layout flip, bug fixes, per-comment actions (report/delete) |
| `components/routes/route-detail-drawer.tsx` | Pass userId/isAdmin props, rewrite inline preview |

### Architecture Decisions
- **ReportCommentDialog reuse**: The existing dialog from `route-hazard-dialogs.tsx` is imported directly into the discussion panel. It tracks `reportingCommentId` state — when non-null, the dialog opens for that comment.
- **No confirmation on delete**: Soft delete (sets `deleted_at`) is safe and recoverable. No extra dialog needed.
- **Native scroll instead of ScrollArea**: The shadcn ScrollArea (Radix) can clip DropdownMenu portals. Native `overflow-y-auto` avoids this.
- **No API changes**: All three endpoints (GET, POST, DELETE comments + POST report) already work correctly.

### Risk Notes
- Bug fixes may cause previously-posted comments with empty `body` to show correctly now (if any were saved with the correct field despite the frontend bug)
- Three-dot opacity-0 on desktop means the button is invisible but still occupies space — this is acceptable since it's a small 7x7 element

### Verification
1. `npm run build` — passes
2. Inline preview shows up to 3 comments with correct `body` text
3. "View all N comments" link appears when >3 comments
4. Full panel: input at bottom, comments scroll above
5. Posting a comment works (sends `body` field correctly)
6. Three-dot menu: Report opens dialog with correct commentId
7. Three-dot menu: Delete removes own comment (soft delete)
8. Empty state shows when no comments
9. Bottom bar hides during discussion panel

### What's Next — Remaining Phases
- **Phase 4**: Review flow simplification (Star → Photos → Tags → Warnings → Thank you, no text)
- **Phase 5**: Tabbed section (Map[mobile] / Elevation / Waypoints / Hazards / Warnings / Weather)
- **Phase 6**: Waypoint contributions (users add photos + descriptions to waypoints)

---

## Session 6: Route Detail Drawer — Phase 4 Review Flow Simplification

### Summary
Simplified the review flow from 5 steps to 3+1. Removed all text input (short note, long note). Flow is now: Stars → Photos → Tags → Submit → Warnings/Thank You. This reduces friction and gets riders through reviews faster.

### Changes

**Removed:**
- Step 4 (Short Note textarea, 500-char limit) — entire step deleted
- Long note section from step 5 (textarea, "Share more", "Save Note" button)
- `reviewShortNote` and `reviewLongNote` state variables
- `handleSubmitLongNote` handler
- `Textarea` and `Separator` imports (no longer needed)
- Pre-populate logic for `short_note` and `long_note` when editing

**Modified:**
- Step 3 (Tags) "Next" button → "Submit Review" button (now calls `handleSubmitRideReview`)
- `handleSubmitRideReview`: notes JSON simplified to `{ tags: [...] }` (no `short_note`, no `long_note`), review POST no longer sends `review_text`
- Step 5 → renumbered to step 4 (warnings + thank you)
- Step dots: 4 → 3 (the thank you screen is post-submit, not a navigable step)
- Step indicator: "Step X of 4" → "Step X of 3", "Complete!" for step 4
- `maxVisitedStep` init: `5 → 4` for returning users
- Thank you screen: just celebration + tag badges + "Done" button (no text input)

**File reduced from 646 to 428 lines.**

### Files Modified

| File | Changes |
|------|---------|
| `components/routes/route-review-flow.tsx` | Remove text steps, renumber, simplify submission |

### Architecture Decisions
- **No API changes**: The `/complete` and `/review` endpoints accept optional text fields. New reviews simply omit them, sending `notes: { tags: [...] }`.
- **Old reviews preserved**: Reviews with `short_note`/`long_note` still display correctly in `route-review-cards.tsx` (no changes to display component).
- **Submit on step 3**: The review is submitted when leaving the tags step. The warnings/thank you screen (step 4) is a post-submit confirmation, not a data collection step.

### Risk Notes
- No data loss — old reviews with text still render in review cards
- New reviews will have empty `short_note`/`long_note` in the notes JSON (or absent)
- The `/review` API creates `route_reviews` without `review_text` — the field is nullable

### Verification
1. `npm run build` — passes
2. Flow shows 3 step dots (was 4)
3. Step 1: Stars, Step 2: Photos, Step 3: Tags + Submit
4. No text input anywhere in the flow
5. "Submit Review" on step 3 submits and advances to thank you
6. Warning check still works (if active warnings)
7. Thank you shows tags, "Done" closes flow
8. Old reviews with text notes still display in review cards
9. Edit mode pre-populates rating and tags (no text)

### What's Next — Remaining Phases
- **Phase 5**: Tabbed section (Map[mobile] / Elevation / Waypoints / Hazards / Warnings / Weather)
- **Phase 6**: Waypoint contributions (users add photos + descriptions to waypoints)

---

## Session 7: Route Detail Drawer — Phase 5 Tabbed Section

### Summary
Consolidated 5 scattered vertical sections (elevation profile, map snapshot, weather, active warnings, waypoints/hazards/warnings row) into a compact tabbed interface using shadcn Tabs. The tabs sit after the stats grid and provide organised access to: Map (mobile only), Elevation (default), Waypoints, Hazards, Warnings, and Weather.

### Changes

**New component: `route-detail-tabs.tsx`**
- 6 tabs using shadcn `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent`
- Compact tab triggers with icons + labels (`text-xs`), horizontally scrollable (`overflow-x-auto scrollbar-hidden`)
- Badge counts on Waypoints, Hazards (red), and Warnings (amber) triggers
- **Map tab** (mobile only, `md:hidden`): Static Mapbox thumbnail at h-52
- **Elevation tab** (default): `ElevationProfile` component at h-36 (taller than old h-28), loading skeleton, empty state
- **Waypoints tab**: Compact list of first 6 waypoints with colour dots (green=start, red=finish, blue=waypoint), elevation, distance. "View full waypoint list" button opens full panel
- **Hazards tab**: Active hazard cards with severity styling, "View on map" button, "Report a hazard" button
- **Warnings tab**: Full warning cards with vote buttons (ported from inline warnings), "Post a warning" button, show more/less for >3 warnings
- **Weather tab**: Pass-through to `RouteWeatherSection`

**Drawer changes (`route-detail-drawer.tsx`)**
- Removed 5 inline sections: elevation profile (lines 972-982), map snapshot (lines 984-993), weather section (lines 995-996), active warnings (lines 1119-1163), waypoints/hazards/warnings row (lines 1165-1212)
- Inserted single `<RouteDetailTabs>` component after stats grid
- Removed 4 unused imports: `getMapboxThumbnailUrl`, `ElevationProfile`, `RouteWeatherSection`, `WARNING_TYPES`/`getTimeRemaining`
- Added import for `RouteDetailTabs`

### Files Created

| File | Purpose |
|------|---------|
| `components/routes/route-detail-tabs.tsx` | Tabbed section with 6 tabs for route detail drawer |

### Files Modified

| File | Changes |
|------|---------|
| `components/routes/route-detail-drawer.tsx` | Removed 5 scattered sections, added tabs component, cleaned up imports |

### Architecture Decisions
- **Default tab is "elevation"**: Most universally useful — every route with geometry has elevation data. Map is mobile-only since desktop users already see the full map.
- **Warning show/hide threshold at 3**: In the tab context (more space than inline), we show up to 3 warnings before collapsing. The inline version showed only 1.
- **`warningDialogOpen` state stays in drawer**: Tabs calls `onPostWarning` callback which triggers `setWarningDialogOpen(true)` in the drawer. Keeps dialog management centralised.
- **No API or DB changes**: All data is already fetched by the drawer and passed down as props.

### Risk Notes
- Map tab hidden on desktop (`md:hidden`) — desktop users see the map in the full Mapbox component, not a static thumbnail
- Elevation loading skeleton and empty state added (was previously just hidden when no data)
- All hazard/warning functionality preserved via callbacks

### Verification
1. `npm run build` — passes
2. Tabbed section appears after stats grid
3. Elevation tab shows chart (default active)
4. Map tab visible only on mobile, shows route snapshot
5. Waypoints tab shows compact list + "View full waypoint list" opens full panel
6. Hazards tab shows active hazards + "Report a hazard" button works
7. Warnings tab shows warning cards with vote buttons + "Post a warning" works
8. Weather tab shows current conditions and forecast
9. Badge counts on tab triggers update correctly
10. All removed inline sections no longer appear outside tabs

### What's Next — Remaining Phases
- **Phase 6**: Waypoint contributions (users add photos + descriptions to waypoints)

---

## Session 8: Route Detail Drawer — Phase 6 Waypoint Contributions

### Summary
Added community waypoint contributions: multiple photo uploads per waypoint, inline description editing with permission checks, and GPS-based "Add waypoint" from the trail. Also fixed a permission bug where waypoint creators and admins were blocked from editing/deleting waypoints they created.

### Bug Fix
**Waypoint PATCH/DELETE permission bug** (`app/api/routes/[id]/waypoints/[waypointId]/route.ts`)
- Both PATCH and DELETE only checked `route.owner_user_id !== user.id`, blocking waypoint creators and admins
- RLS policies already allowed creator/owner/admin — the API-level check was more restrictive
- Fixed to check: route owner OR waypoint creator (`created_by_user_id`) OR admin

### Changes

**Migration: `070_waypoint_photos.sql`**
- New `waypoint_photos` table: id, waypoint_id, route_id, user_id, url, caption, created_at
- Indexes on waypoint_id and route_id
- RLS: public read, authenticated insert (own user_id), delete by uploader/owner/admin

**Waypoint photos API (`app/api/routes/[id]/waypoints/[waypointId]/photos/route.ts`)**
- GET: Fetch all photos for a waypoint with user join (name, avatar)
- POST: Multipart form-data upload. Validates image type + 10MB limit. Stores in `route-photos` bucket at `{routeId}/waypoints/{waypointId}/{userId}/{timestamp}.{ext}`. Uses service client for DB insert.
- DELETE: Remove own photo, or admin/owner can delete any photo

**AddWaypointDialog (`components/routes/add-waypoint-dialog.tsx`)**
- GPS proximity check (500m threshold, same as HazardReportDialog)
- Location status banners: checking/near/far/error
- Form fields when "near": name (required), tag (select), icon type (select), description (optional)
- Submits to existing POST `/api/routes/{routeId}/waypoints` with GPS lat/lng

**Waypoints panel rewrite (`components/routes/route-waypoints-panel.tsx`)**
- New props: routeId, userId, isOwner, isAdmin, routeOwnerId, routeGeometry, onWaypointAdded, onWaypointUpdated
- **Photos**: Lazy-loaded when waypoint expanded. 3-column grid of square thumbnails. Delete button (X) on own photos or owner/admin. "Add photo" button with hidden file input.
- **Description editing**: Pencil icon for authorized users (waypoint creator, route owner, admin). Inline textarea + Save/Cancel. "+ Add description" for empty descriptions. Uses existing PATCH endpoint.
- **Add waypoint**: "Add" button in header opens AddWaypointDialog
- **Action buttons**: "Add photo" and "Show on map" side by side

**Drawer wiring (`components/routes/route-detail-drawer.tsx`)**
- Passes new props to RouteWaypointsPanel: routeId, userId, isOwner, isAdmin, routeOwnerId, routeGeometry, onWaypointAdded, onWaypointUpdated

### Files Created

| File | Purpose |
|------|---------|
| `supabase/migrations/070_waypoint_photos.sql` | New table + RLS for waypoint photos |
| `app/api/routes/[id]/waypoints/[waypointId]/photos/route.ts` | GET/POST/DELETE for waypoint photos |
| `components/routes/add-waypoint-dialog.tsx` | GPS-based community waypoint creation dialog |

### Files Modified

| File | Changes |
|------|---------|
| `app/api/routes/[id]/waypoints/[waypointId]/route.ts` | Fix permission bug: allow creator + admin |
| `components/routes/route-waypoints-panel.tsx` | Photo grid, upload, description editing, add button |
| `components/routes/route-detail-drawer.tsx` | Pass new props to waypoints panel |

### Architecture Decisions
- **Lazy photo loading**: Photos fetched per-waypoint when expanded (not all at once). Avoids loading hundreds of photos for routes with many waypoints.
- **Reuse `route-photos` bucket**: Waypoint photos stored under `{routeId}/waypoints/` subfolder. No new bucket needed.
- **No moderation queue**: Photos appear immediately, consistent with route photos and comments. Owner/admin can delete.
- **Description protection**: Only waypoint creator, route owner, or admin can edit descriptions. No moderation needed.
- **GPS proximity for new waypoints**: 500m threshold ensures waypoints are placed at relevant locations. Matches hazard reporting pattern.

### SQL Migration to Run
```sql
-- Run in Supabase SQL Editor:
-- Copy contents of supabase/migrations/070_waypoint_photos.sql
```

### Verification
1. `npm run build` — passes
2. Migration SQL runs in Supabase without errors
3. Open waypoint panel, expand a waypoint → see "Add photo" button
4. Upload a photo → appears in community photos grid
5. Delete own photo → disappears from grid
6. Waypoint creator sees pencil icon on description → edit inline → saves
7. Route owner sees pencil icon on ANY waypoint's description
8. Non-owner/non-creator does NOT see pencil icon
9. "Add" button in panel header → dialog opens → GPS check runs
10. When near route, fill form and submit → new waypoint appears in list
11. When far from route, see distance warning and submit is blocked

---

## Session 9: Route Detail Drawer — Phase 7 Komoot-Style Layout Rework

### Summary
Major layout rework based on user feedback comparing the deployed build against Komoot inspiration. Addressed 13 specific issues: removed white card border, moved buttons to photo hero (X top-left, share/like/more top-right), added rounded content overlap, reworked stats to 2x2 grid, made description plain text, replaced tabs with permanent inline sections, added waypoint/hazard markers on elevation chart, inlined discussion as Komoot-style comments, removed rider reviews and safety notice, moved nearby properties to bottom, and consolidated secondary actions into a dropdown menu.

### Changes

**Elevation profile markers (`elevation-profile.tsx`)**
- Added optional `waypoints` and `hazards` props for rendering markers on the chart
- Waypoint markers: blue dots with name labels (HTML overlay)
- Hazard markers: amber warning triangles (HTML overlay)
- New `waypointMarkers` and `hazardMarkers` useMemo computations

**Drawer full restructure (`route-detail-drawer.tsx`)**
- **White border removed**: Deleted `border border-gray-100` from modal wrapper
- **Close button**: Moved from modal wrapper to photo hero top-left
- **Share + Like + More**: Grouped at photo hero top-right. More button is a DropdownMenu with Export GPX, Copy & Edit (owner/admin only), Report
- **Content overlap**: Content area wraps in `rounded-t-2xl -mt-4 relative z-10 bg-white` when photos exist (Komoot-style overlap)
- **Stats**: Changed from `grid-cols-4` to `grid-cols-2` with `text-2xl font-bold` values and unit suffixes
- **Description**: Removed `bg-slate-50 rounded-lg p-3` wrapper, now plain text with "Show more/Show less"
- **Discussion**: Replaced bordered card with inline Komoot-style comments section (avatar, name, timestamp, body). "Add a comment" link opens full panel
- **Elevation chart**: Always visible (not a tab), increased from `h-36` to `h-48`, receives waypoint and hazard markers
- **Waypoint list**: Permanent section below chart with "View all" header link
- **Hazard list**: Permanent section with severity cards and "Report a hazard" button
- **Warnings**: Inline section (not a tab) with vote buttons and "Post a warning"
- **Weather**: Inline section (not a tab)
- **Removed**: RouteReviewCards, safety notice, secondary actions bar
- **Moved**: Nearby Properties to bottom of content
- **New imports**: TrendingUp, Plus, Cloud, MoreHorizontal, Skeleton, ElevationProfile, RouteWeatherSection, DropdownMenu, HAZARD_TYPES, SEVERITY_COLORS, WARNING_TYPES, getTimeRemaining
- **Removed imports**: RouteDetailTabs, RouteReviewCards
- **Removed state**: showAllReviews
- **New computed**: hazardsWithDistance useMemo (computes distance along route + elevation for each hazard)

**Deleted: `route-detail-tabs.tsx`**
- No longer imported after restructure. All tab content now renders inline.

### Files Modified

| File | Changes |
|------|---------|
| `components/routes/elevation-profile.tsx` | Added waypoint/hazard marker props and rendering |
| `components/routes/route-detail-drawer.tsx` | Full layout restructure (all 13 feedback items) |

### Files Deleted

| File | Reason |
|------|--------|
| `components/routes/route-detail-tabs.tsx` | Orphaned — no longer imported after inline restructure |

### Architecture Decisions
- **Tabs removed entirely**: Elevation, waypoints, hazards, warnings, and weather now render as permanent inline sections. This matches the Komoot layout where key route data is always visible without tab switching.
- **Hazard distance computation**: `hazardsWithDistance` memo finds each hazard's nearest route coordinate (O(hazards × coords)), computes cumulative distance along route, then looks up elevation from the sampled elevation profile. This allows placing hazard markers on the elevation chart.
- **DropdownMenu for secondary actions**: Export GPX, Copy & Edit, and Report consolidated into a "..." dropdown on the photo hero. Reduces visual clutter.
- **Inline discussions**: Comments preview renders directly in the content flow, not wrapped in a bordered card. This matches Komoot's comment style. Full discussion panel still accessible via "Add a comment" or "View all" links.

### Risk Notes
- Hazard markers on elevation chart depend on hazards having lat/lng coordinates. Hazards without coordinates are filtered out.
- Waypoint markers depend on `waypointElevationMap` having data. Waypoints without elevation are filtered out.
- The `-mt-4` content overlap only applies when photos exist (conditional class), avoiding visual issues on routes without photos.

### Verification
1. `npm run build` — passes
2. Photo hero: X top-left, share/like/more dropdown top-right
3. No white border around card
4. Content overlaps photo hero with rounded top edge
5. Stats render as 2x2 grid with large numbers
6. Description is plain text with show more/less
7. Comments appear inline between description and elevation
8. Elevation chart is bigger (h-48), always visible, shows waypoint dots and hazard triangles
9. Waypoint list appears permanently below chart
10. Hazard list appears with "Report a hazard" button
11. Warnings and Weather render as inline sections
12. Rider Reviews section is gone
13. Safety notice is gone
14. Nearby Properties appears at the bottom
15. Secondary actions accessible via "..." dropdown on photo hero

---

## Session 10: Route Detail Drawer — Post-Deploy Feedback Round 2

Addressed 6 issues identified from testing the deployed Phase 7 build.

### Changes

**`components/routes/route-detail-drawer.tsx`**
1. **White card overlap reduced** — Changed `-mt-4` to `-mt-2` so the "See all" photo count button no longer clashes with the rounded card top
2. **Difficulty badge smaller** — Reduced from `text-sm px-3 py-1` to `text-xs px-2 py-0.5`
3. **Description above stats** — Swapped order so description renders before the 2x2 stats grid
4. **Minimal Komoot-style social bar** — Replaced full inline comment previews with overlapping avatar circles, like/comment counts, and a "Comment" button. Clicking opens the full discussion panel
5. **Tab-like header bar** — Replaced stacked sections (elevation, waypoints, hazards, warnings, weather) with a horizontal tab header bar. Underlined active tab with green highlight. Content area below changes based on selected tab:
   - Elevation: shows plain elevation chart
   - Waypoints: shows chart with waypoint markers + waypoint list below
   - Hazards: shows chart with hazard markers + hazard list below
   - Warnings: replaces chart with warning cards
   - Weather: replaces chart with weather section
6. **Nearby Properties always visible** — Section now renders even when no properties are found (shows "No nearby stays found for this route" empty state instead of being hidden)

Added `activeInfoTab` state variable to control which tab is selected (defaults to "elevation").

### Risk Notes
- Tab header scrolls horizontally on small screens (`overflow-x-auto`)
- Elevation chart is shared across elevation/waypoints/hazards tabs — rendered 3 times but only one is visible at a time (conditional rendering, no performance concern)

### Verification
1. `npm run build` — passes
2. White card overlap no longer clashes with "See all" button
3. Difficulty badge is smaller and proportional
4. Description appears above statistics
5. Comments section is a minimal social bar (avatars + counts)
6. Tab header bar shows: Elevation | Waypoints | Hazards | Warnings | Weather
7. Clicking tabs changes the content below
8. Nearby Properties section always visible at the bottom

---

## Session 11: Phase 8 — Floating Chart Markers + Komoot-Style Waypoint Timeline

Added Komoot-style floating markers above the elevation chart with dotted drop-lines, and a permanent waypoint timeline below the tabbed section.

### Changes

**`app/api/routes/[id]/waypoints/route.ts`** — Enriched waypoints API
- Changed GET query from `select("*")` to `select("*, waypoint_photos(id, url, caption, created_at)")`
- Post-processes to return `photo_count` and `community_photos` (first 3, newest first) per waypoint
- Strips raw `waypoint_photos` join data from response

**`components/routes/elevation-profile.tsx`** — Floating marker mode
- Added `markerStyle?: "inline" | "floating"` prop (default "inline" for backward compat)
- Added `waypointItems`, `hazardItems`, `onWaypointClick`, `onHazardClick` props
- When `markerStyle="floating"`: container splits into marker gutter (`h-10`) + chart area (`h-40`)
- Floating waypoint markers: numbered circles (green A=start, blue 1/2/3=waypoints, red B=finish)
- Floating hazard markers: amber warning triangles
- Dotted drop-lines from gutter to elevation line with small dots at intersection points
- Stagger algorithm: markers within 5% x-distance bump vertically (max 2 tiers)
- Key-point markers suppressed in floating mode (floating markers serve that purpose)
- Clickable markers fire `onWaypointClick`/`onHazardClick` callbacks

**`components/routes/waypoint-timeline.tsx`** — New Komoot vertical timeline
- Permanent section below tabbed area, always visible
- Vertical dotted connectors between entries with distance labels (`_distFromPrev`)
- Numbered circles: green "A" (start), blue "1/2/3" (waypoints), red "B" (finish)
- Tag badges (Instruction/POI/Caution) using TAG_CONFIG
- Description preview (line-clamp-2)
- Photo thumbnails: primary photo + up to 2 community photos (40px squares) + count
- Each entry has `id="waypoint-timeline-{wp.id}"` for scroll-to from chart
- "View all" button opens full waypoints panel
- Clicking any waypoint calls `onFlyToLocation`

**`components/routes/route-detail-drawer.tsx`** — Integration
- Imported `WaypointTimeline`
- Added `handleChartWaypointClick` callback: maps chart marker index → timeline entry → scrollIntoView
- Elevation tab: clean chart at h-56, no markers
- Waypoints tab: floating waypoint markers on chart, compact list removed
- Hazards tab: floating hazard markers on chart, id attributes on hazard cards for scroll-to
- Permanent `<WaypointTimeline>` section between tab content and "I've ridden this route" button
- Chart height increased from h-48 to h-56 across all tabs

### Files Created

| File | Purpose |
|------|---------|
| `components/routes/waypoint-timeline.tsx` | Komoot vertical timeline with connectors, photos, numbered circles |

### Files Modified

| File | Changes |
|------|---------|
| `app/api/routes/[id]/waypoints/route.ts` | Join waypoint_photos, add photo_count + community_photos |
| `components/routes/elevation-profile.tsx` | Floating marker mode with gutter, drop-lines, stagger, clicks |
| `components/routes/route-detail-drawer.tsx` | Wire floating charts, add permanent timeline, remove compact list |

### Verification
1. `npm run build` — passes
2. Elevation tab: clean chart, no markers
3. Waypoints tab: chart with floating numbered markers above, dotted drop-lines
4. Hazards tab: chart with floating amber triangles above, dotted drop-lines
5. Clicking a waypoint marker on chart scrolls to timeline entry below
6. Permanent timeline below tabs with dotted connectors, distance labels, photos
7. Chart is taller (h-56 vs h-48)
8. Overlapping markers stagger vertically
