# Changelog — 2026-02-28

## Session: Spring Cleaning — Custom Commands, QA Rules, Code Review Fixes

Set up developer tooling, added mandatory quality checks to CLAUDE.md, and fixed issues found during code review.

---

### 1. Custom Slash Commands

**Files created:**
- `.claude/commands/discuss.md` — `/discuss` command for strategic planning and architecture discussions. Provides a structured framework: context, current state, options, tradeoffs, recommendation, open questions.
- `.claude/commands/uxamine.md` — `/uxamine` command for deep UI/UX examination. Includes a full checklist covering visual design, interaction design, information architecture, responsiveness, and accessibility. Outputs structured findings with severity ratings.

**Fix:** Commands initially failed with "Unknown skill" error because they were missing YAML frontmatter. Added `---` frontmatter blocks with `name`, `description`, and `argument-hint` fields to both files. Commands now register correctly (requires session restart to pick up).

### 2. CLAUDE.md Quality Rules

**File modified:** `CLAUDE.md`

Added two new mandatory sections:

**Post-Change Quality Checks** — after every implementation:
- Diff review for bugs, debug code, security issues
- Functionality preservation check
- Documentation and design alignment
- Test alignment verification
- Task completion cross-check

**Post-Change Workflow** — after completing work:
1. Build check (`npm run build`)
2. Full summary of changes
3. Changelog update with correct date
4. Git commit with conventional commit style and push
5. Report concerns/recommendations

Also added **Custom Commands** reference table.

### 3. Code Review Fixes

Issues found during systematic review of recent changes:

**Fix: Photos route storage client (security hardening)**
**File:** `app/api/routes/[id]/photos/route.ts`

Reverted storage upload from service client back to regular authenticated client. The storage bucket policy already allows authenticated uploads (`auth.role() = 'authenticated'`). The service client should only be used for database inserts where RLS blocks the operation. This follows the principle of least privilege — storage operations use the user's auth context, only DB operations bypass RLS.

**Fix: Normalize photo timestamps in completions API**
**File:** `app/api/routes/[id]/completions/route.ts`

Photos from `route_user_photos` have an `uploaded_at` column while `route_photos` has `created_at`. When merging photos from both tables, the timestamp field names were inconsistent. Fixed by mapping `uploaded_at` → `created_at` on user photos so all photos have a uniform `created_at` field. Also added time-based sorting so photos from both sources are properly ordered (newest first).

---

### Files Created

| File | Purpose |
|------|---------|
| `.claude/commands/discuss.md` | `/discuss` slash command definition |
| `.claude/commands/uxamine.md` | `/uxamine` slash command definition |

### Files Modified

| File | Changes |
|------|---------|
| `CLAUDE.md` | Added Post-Change Quality Checks, Post-Change Workflow, Custom Commands sections |
| `app/api/routes/[id]/photos/route.ts` | Reverted storage upload to regular client (service client for DB only) |
| `app/api/routes/[id]/completions/route.ts` | Normalized photo timestamps, added sort |

### Test Impact

- No tests were modified or broken
- Existing tests (4 active Playwright E2E tests) remain valid
- Test coverage was not reduced — recent features (weather, photo upload, reviews) have no test coverage (pre-existing gap)

### Verification

1. `npm run build` — passes
2. Photo upload still works (regular client has authenticated upload policy)
3. Photo deletion still works (service client for DB operations)
4. Completions API returns photos with consistent `created_at` field, sorted newest first
5. `/discuss` and `/uxamine` commands are available in Claude Code sessions

---

## Recent Context (for next session continuity)

This section summarises the broader state of the project so that the next Claude session can pick up where this one left off.

### What was done in the 2026-02-26 session

1. **Weather conditions added to route detail drawer** (`lib/weather.ts`, `app/api/weather/route.ts`, `components/routes/route-detail-drawer.tsx`, `lib/cache.ts`)
   - Uses free Open-Meteo API (no key needed). Server-side endpoint at `/api/weather?lat=...&lng=...` with 30-min in-memory cache.
   - Route detail drawer shows: current conditions (temp, icon, wind, precipitation) in a 4-column grid, 3-day forecast cards, and amber/red warning banners for dangerous conditions (freezing rain, snow, thunderstorms, wind gusts >60km/h — horses spook).
   - Centroid calculated from route geometry to determine weather location.

2. **Photo upload bug fixed** — Root cause: `route_user_photos` table has `uploaded_at` column (from migration 030), but all queries used `created_at`. PostgREST silently returned null for the invalid column, and `data || []` fallback hid the failure. Fixed in `completions/route.ts` and `photos/route.ts`.

3. **Rider reviews system** — Previously built across multiple sessions. Reviews with star ratings, text notes (stored in `route_completions.long_note`), photo grids with lightbox, edit/delete functionality. Stars/ratings are NOT shown publicly — they're for internal use only.

### Key technical notes for the next session

- **Two photo tables**: `route_user_photos` (community/review photos, column: `uploaded_at`) and `route_photos` (owner/admin photos, column: `created_at`). Different timestamp column names — always check which table you're querying.
- **PostgREST silent failures**: Querying a non-existent column returns `{ data: null, error: {...} }`, not a thrown exception. Combined with `data || []` fallback, this silently hides all results. Be careful with column names.
- **Service client usage**: `createServiceClient()` (from `lib/supabase/service.ts`) bypasses RLS. Use ONLY for DB operations where RLS blocks (e.g., cross-user queries). Storage operations should use the regular authenticated client — the bucket policy allows `auth.role() = 'authenticated'`.
- **Route geometry**: Stored as GeoJSON LineString `{ type: "LineString", coordinates: [[lng, lat], ...] }`. The `route` object may have it as `geometry` or `route_geometry` depending on context.

### What's next

**Route main card and detail drawer redesign.** The user and their partner had a meeting and decided they don't like the current route card design. They have a completely redesigned version. The user will share new designs/mockups at the start of the next session. Use `/discuss` to plan the approach and `/uxamine` to audit the result.

Key files that will likely be modified:
- `components/routes/route-detail-drawer.tsx` — the main route detail view (~2700 lines)
- Route card components in `components/routes/`
- Possibly route list/grid layout components

The weather section was just added to the drawer — it should be preserved or adapted to fit the new design.

---

## Session 2: Route Detail Drawer Decomposition

### Summary
Decomposed the monolithic `route-detail-drawer.tsx` (3,712 lines) into 9 focused files totalling 3,310 lines. This is a pure refactor with zero UI or behaviour changes — every feature works exactly as before. Also deleted the accidental `nul` file artifact and added it to `.gitignore`.

### Files Created
| File | Lines | Purpose |
|------|-------|---------|
| `components/routes/route-detail-constants.ts` | 96 | Shared constants: `HAZARD_TYPES`, `WARNING_TYPES`, `REVIEW_TAGS`, `PHOTO_CATEGORIES`, `DIFFICULTY_CONFIG`, `SEVERITY_COLORS`, helper functions (`getDifficultyInfo`, `getTimeRemaining`, `getDistanceMeters`) |
| `components/routes/route-photo-lightbox.tsx` | 88 | Reusable full-screen photo lightbox with swipe navigation, used by both the main carousel and review photos |
| `components/routes/route-weather-section.tsx` | 119 | Weather display: current conditions grid, warning banner, 3-day forecast with skeleton loading |
| `components/routes/route-discussion-panel.tsx` | 130 | Full discussion panel: comment list, posting new comments, reporting |
| `components/routes/route-waypoints-panel.tsx` | 229 | Waypoints accordion panel: tag filtering, OS Grid Refs, elevation, distance calculations, fly-to-location |
| `components/routes/route-hazard-dialogs.tsx` | 593 | Four dialog components: `HazardReportDialog`, `WarningPostDialog`, `ReportCommentDialog`, `DeleteHazardDialog` |
| `components/routes/route-review-flow.tsx` | 646 | 5-step review wizard: star rating, photo upload, tags, short note, warning check/long note |
| `components/routes/route-review-cards.tsx` | 267 | Expandable review cards with photos, tags, and lightbox integration |

### Files Modified
| File | Change |
|------|--------|
| `components/routes/route-detail-drawer.tsx` | Reduced from 3,712 to 1,142 lines. Now serves as orchestrator: state management, data fetching, handlers, and layout composition. Removed dead code (`PhotoCard`, unused loading states, unused dialog state, `refreshPhotos`, `handleResolveHazard`). Removed unused `mobileShowDetails`/`onMobileToggleDetails` props. |
| `app/routes/page.tsx` | Removed `mobileShowDetails` and `onMobileToggleDetails` props from drawer usage (were already dead props) |
| `.gitignore` | Added `nul` to prevent Windows artifact from being tracked |

### Files Deleted
| File | Reason |
|------|--------|
| `nul` | Accidental 0-byte Windows artifact in repo root |

### Architecture Decisions
- **Props/callbacks pattern** for cross-component communication (not React Context) — keeps components testable and self-contained
- **Sub-components manage their own UI state** (expanded items, lightbox, tag filters) while the orchestrator owns data and API state
- **Orchestrator pattern**: `route-detail-drawer.tsx` remains the single entry point, handling all data fetching, auth checks, and passing data down via props
- **Bug fix**: Added `initialExpandedId` prop to `RouteWaypointsPanel` so waypoint auto-expansion works when navigating from a map marker

### Risk Notes
- Pure refactor — no database, API, or UI changes
- All cross-component interactions preserved via props/callbacks
- Build passes cleanly with no errors

### What's Next
- Route card and detail drawer UI redesign (user has new designs/mockups)
- Test coverage improvements (deferred until after redesign)

---

## Session 3: Route Detail Drawer — Phase 1 Core Layout Redesign

### Summary
Redesigned the route detail drawer layout inspired by Komoot. Phase 1 covers the core structural changes: photo hero with overlay buttons, author/meta section with reverse geocoding, reordered content, and a new bottom bar. This is the first of 6 planned phases for the complete redesign.

### Changes

**Photo hero (full-bleed)**
- Taller hero image (h-64 / h-72 on desktop vs old h-52)
- Always renders — shows green gradient placeholder when no photos
- Like button (heart) moved from bottom bar to photo overlay (bottom-right, red when liked)
- Likes count displayed next to heart on photo
- Terrain tags overlaid on photo (bottom-left, frosted glass pills)
- Bottom gradient for text readability over photos
- Photo count badge moved to bottom-left
- Prev/next arrows and dot indicators preserved

**Author + meta section**
- Bigger avatar (h-10 vs h-8), PFP on the left (Komoot-style)
- Two-line layout: name + meta row (date + location)
- Date shown using `updated_at` or `created_at` with `formatDistanceToNow`
- Location name via reverse geocoding (Mapbox Geocoding API, computed from route centroid)

**Content reorder**
- Description now appears before stats (was after)
- Terrain tags conditional: shown on photo when photos exist, shown as badges in content when no photos

**Bottom bar redesign**
- Removed: Like button (moved to photo overlay), Share button (moved to secondary actions)
- Added: Save/Saved button (primary action) + Navigate button (disabled placeholder for future)
- Bottom bar conditionally hidden during review flow, discussion panel, and waypoints panel

**Modal structure**
- Removed: Green gradient header bar with title
- Added: Floating close button (X) — dark frosted glass circle, always visible over scroll content (z-20)
- Scrollable content now starts at the photo hero (no header taking space)

**Secondary actions row**
- Added Share button to the existing GPX / Edit / Report action bar

**Removed**
- Engagement stats grid (likes/completions/shares) — likes now on photo, completions as "times ridden" in meta, shares not needed standalone
- `PHOTO_CATEGORIES` import — no longer used after removing caption category label from photo carousel

### Files Modified

| File | Changes |
|------|---------|
| `components/routes/route-detail-drawer.tsx` | Photo hero, author+meta, content reorder, bottom bar, modal structure, reverse geocoding |

### Architecture Decisions
- **Reverse geocoding on client**: Uses `NEXT_PUBLIC_MAPBOX_TOKEN` directly from client to call Mapbox Geocoding API. One request per drawer open, no caching (route centroid is stable per route). Uses `types=place,locality` and takes first two parts of `place_name` for readable location (e.g., "Dartmoor, Devon").
- **Conditional bottom bar**: Hides during sub-panels and review flow so those views get full vertical space.
- **Floating close button**: Positioned absolutely in the modal card with z-20. Works over both photo hero and white content. Sub-panels have their own back buttons; the X is an escape hatch.

### Risk Notes
- `route.updated_at` is accessed but TypeScript types are stale (field exists in DB per migration 019, auto-update trigger active). Works because `route` is typed as `any`.
- Reverse geocoding adds one Mapbox API call per drawer open — negligible cost, no rate limit concern.
- Navigate button is a disabled placeholder — will be wired up in a future phase.

### Verification
1. `npm run build` — passes
2. Photo hero renders with overlay buttons and terrain tags
3. Author section shows date and reverse-geocoded location
4. Description appears before stats
5. Bottom bar shows Save + Navigate
6. Floating X close button works over photo and content
7. Share moved to secondary actions row
8. All sub-panels (discussion, waypoints, review flow) still functional
9. Like/unlike from photo overlay preserves optimistic update behaviour

### What's Next — Remaining Phases
- **Phase 2**: Photo system (collective pool, collage takeover, cover management)
- **Phase 3**: Comments + social (inline preview, full takeover, reporting)
- **Phase 4**: Review flow simplification (Star → Photos → Tags → Warnings → Thank you, no text)
- **Phase 5**: Tabbed section (Map[mobile] / Elevation / Waypoints / Hazards / Warnings / Weather)
- **Phase 6**: Waypoint contributions (users add photos + descriptions to waypoints)

---

## Session 4: Route Detail Drawer — Phase 2 Photo System

### Summary
Added a collective photo gallery takeover showing all photos (owner + community) in a unified grid. Accessible via the "See all N" badge on the photo hero. Owners can set cover photos from the gallery. Photo lightbox works from both hero carousel and gallery contexts.

### Changes

**Photo gallery takeover (`route-photo-gallery.tsx`)**
- Full-screen gallery panel matching the discussion/waypoints panel pattern
- Header with back button and photo count badge
- 2-column (3 on desktop) grid of square thumbnails
- Cover badge (green, star icon) on the current cover photo
- Community badge (blue, users icon) on community-uploaded photos
- "Set as cover" hover button for owners (only on owner photos)
- Photo info: caption, uploader avatar + name, relative timestamp
- Empty state with prompt to add photos via reviews
- Click any thumbnail to open the lightbox at that index

**Drawer orchestrator changes (`route-detail-drawer.tsx`)**
- Added `userPhotos` state — captures community photos from the existing API response (was returned but ignored)
- Expanded `activeFullPanel` type to include `"photos"`
- Added `allPhotosForGallery` useMemo — normalizes both `route_photos` (owner, `created_at`) and `route_user_photos` (community, `uploaded_at`) into a unified sorted array
- Added `totalPhotoCount` derived value (owner + community)
- Added `handleSetCoverPhoto` handler — calls existing PATCH endpoint, re-fetches all photos on success
- Photo count badge changed from static `"1/3"` display to clickable `"See all N"` button that opens gallery
- Added `"photos"` branch in render conditional (after waypoints, before main content)
- PhotoLightbox now uses `allPhotosForGallery` when gallery is active, `displayPhotosForCarousel` otherwise

### Files Created

| File | Purpose |
|------|---------|
| `components/routes/route-photo-gallery.tsx` | Photo gallery takeover component |

### Files Modified

| File | Changes |
|------|---------|
| `components/routes/route-detail-drawer.tsx` | userPhotos state, gallery memo, cover handler, clickable badge, photos panel branch, lightbox source switch |

### Architecture Decisions
- **Photo normalization**: Both photo tables have different column names (`created_at` vs `uploaded_at`, `uploaded_by_user_id` vs `user_id`, `uploader` vs `user` joins). The `allPhotosForGallery` memo normalizes these into a single `GalleryPhoto` interface sorted by date.
- **Cover management restricted to owner photos**: Only `route_photos` items can be set as cover (not community photos), enforced in UI via `canSetCover` check.
- **Lightbox context switching**: The single `PhotoLightbox` instance switches its photo source based on `activeFullPanel === "photos"` — no duplicate lightbox needed.
- **No API or DB changes**: The GET endpoint already returned `userPhotos` and the PATCH endpoint already handles cover management.

### Risk Notes
- Pure additive — no existing behaviour changed
- Community photos may have null `url` if uploaded before URL storage was added (unlikely but `Image` will show broken state)
- Cover photo PATCH requires owner/admin auth (enforced server-side)

### Verification
1. `npm run build` — passes
2. Photo count badge shows "See all N" with total count and is clickable
3. Clicking badge opens gallery takeover with all photos in grid
4. Cover badge shown on cover photo, community badge on user photos
5. Owner sees "Set as cover" on hover for owner photos
6. Clicking gallery thumbnail opens lightbox at correct index
7. Back button returns to main drawer content
8. Hero carousel still works (click photo = lightbox)
9. Bottom bar hides during gallery (existing `activeFullPanel === null` check)

### What's Next — Remaining Phases
- **Phase 3**: Comments + social (inline preview, full takeover, reporting, no likes, no reply UI)
- **Phase 4**: Review flow simplification (Star → Photos → Tags → Warnings → Thank you, no text)
- **Phase 5**: Tabbed section (Map[mobile] / Elevation / Waypoints / Hazards / Warnings / Weather)
- **Phase 6**: Waypoint contributions (users add photos + descriptions to waypoints)
