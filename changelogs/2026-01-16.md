# Changelog - 2026-01-16

## Session Summary

Codebase audit against new rulesets, fee structure correction, routing conflict fix, and UI improvements.

---

## Changes Made

### 1. Fee Structure Correction (Session 1)

**Why:** Two different fee structures existed in the codebase causing potential financial inconsistency.

**Initial fix:** Standardized to 9.5% guest fee (with £150 cap) and 2.5% host fee.

**Files modified:**
- `lib/fees.ts`
- `app/api/booking/payment/route.ts`
- `lib/fees.test.ts`

---

### 2. Next.js Routing Conflict Fix (Session 1)

**Why:** Server wouldn't start due to conflicting dynamic route parameter names.

**Problem:** Two folders under `app/api/properties/`:
- `[id]/` - used by share endpoint
- `[propertyId]/` - used by blocked-dates and nearby-routes

**Solution:** Standardized on `[id]`

**Files created:**
- `app/api/properties/[id]/blocked-dates/route.ts`
- `app/api/properties/[id]/nearby-routes/route.ts`

**Files deleted:**
- `app/api/properties/[propertyId]/` (entire folder)

---

### 3. Changelog System Added (Session 1)

**Why:** Improve auditability and team communication.

**Files created:**
- `changelogs/README.md`
- `changelogs/2026-01-16.md`
- `changelogs/HISTORICAL_SUMMARY.md`

**Ruleset updated:**
- Rule 13 (Changelog Discipline) added to `.cursor/rules/custom-rule.mdc`

---

### 4. Bug Fix: Admin Inspect Dashboard (Session 2 ~14:30 UTC)

**Why:** Runtime error "ReferenceError: selectUser is not defined" when clicking user search results.

**File modified:**
- `components/admin/inspect-dashboard.tsx`

**Fix:**
- `selectUser(user.id)` → `fetchUserData(user.id)`
- `selectProperty(property.id)` → `fetchPropertyData(property.id)`

---

### 5. UI: Move Claims to Bottom of Dropdown (Session 2)

**Why:** User requested Claims be at bottom of dropdown menu.

**File modified:**
- `components/header.tsx`

**Change:** Moved Claims menu item from position 3 to just before the logout separator.

---

### 6. Fee Structure: Remove Cap (Session 2) - SUPERSEDES #1

**Why:** Fee cap was incorrectly implemented. Fees are now UNCAPPED.

**Authoritative rates (FINAL):**
- Guest fee: **9.5%** (NO CAP)
- Host fee: **2.5%** (NO CAP)

**Files modified:**
- `lib/fees.ts` - Removed cap logic, updated comments
- `app/api/booking/payment/route.ts` - Removed cap constant and Math.min()
- `lib/fees.test.ts` - Updated test to verify no cap is applied

---

### 7. UI: Remove USER Verification Icons from Recent Activity (Session 2)

**Why:** User verification display not needed in Recent Activity Feed. Property verification is still important for admin visibility.

**Files modified:**
- `components/admin/analytics-dashboard.tsx`
- `components/admin/analytics-dashboard-new.tsx`

**Change:** Removed verification icons from Recent Users section ONLY. Property verification icons retained (green checkmark = verified, yellow = pending).

---

### 8. New Activity Feed - Social Media Style (Session 3)

**Why:** Previous activity feed was basic. New design is comprehensive, Instagram-like, and shows all platform activity.

**Files created:**
- `app/api/admin/activity-feed/route.ts` - Comprehensive activity API
- `components/admin/activity-feed.tsx` - Social media style feed component

**Features:**
- Unified timeline of all activity types
- Filter by: Bookings, Users, Properties, Reviews, Routes, Moderation
- Profile pictures clickable → profile page
- Properties clickable → property page
- Removed properties shown with strikethrough and "Removed" badge
- Date grouping (Today, Yesterday, older dates)
- Real-time refresh button
- Load more pagination

**Activity types tracked:**
- Bookings: requested, accepted, declined, cancelled, completed
- Users: registered
- Properties: created, verified, removed
- Reviews: posted
- Routes: created
- Claims: filed
- Messages: flagged

---

### 9. Admin User Management Actions (Session 3)

**Why:** Admins need secure ways to help users without accessing passwords.

**Files created:**
- `app/api/admin/user-actions/route.ts` - Handles all admin user actions
- `supabase/migrations/051_admin_audit_log.sql` - Audit log table

**Files modified:**
- `components/admin/inspect-dashboard.tsx` - Added action buttons

**Features added to User Inspect:**
1. **Send Password Reset** - Sends password reset email to user
2. **Send Magic Link** - Sends passwordless login link
3. **Login as User** (Impersonation) - Generates one-time login link for admin to access user's account

**Security:**
- All actions require admin role
- All actions are audit logged with:
  - Admin ID and name
  - Target user ID and email
  - Action type
  - Timestamp
  - Additional details
- Impersonation shows confirmation dialog with warning
- Opens in new tab (admin session preserved)

---

### 10. Impersonation Flow Fix (Session 3-4)

**Why:** "Login as User" feature was not working - admin was being redirected to sign-in instead of logging in as target user.

**Files created:**
- `app/auth/impersonate/page.tsx` - Dedicated impersonation handler

**Files modified:**
- `app/api/admin/user-actions/route.ts` - Updated to return magic link directly
- `app/auth/callback/route.ts` - Added token_hash handling for magic links

**Fix:**
1. Admin clicks "Login as User" → Opens new tab to `/auth/impersonate?magicLink=...`
2. Impersonate page signs out current session
3. Extracts token from magic link and uses `verifyOtp` to authenticate
4. Redirects to dashboard as target user

**Hydration Error Fix:**
- Fixed nested `<p>` tags in AlertDialogDescription (changed to `<span className="block">`)

---

### 11. Admin Dashboard UI Fixes (Session 4 ~12:00 UTC)

**Why:** Multiple UI inconsistencies and missing features reported by user.

#### 11a. Moderation Queue Tab Counters

**File modified:** `components/admin/admin-moderation-queue.tsx`

**Fix:** All tabs now consistently show counters:
- High Priority: Red badge when > 0, "(0)" when empty
- Medium: Amber badge when > 0, "(0)" when empty
- Low: Always shows count in parentheses

---

#### 11b. Damage Claims Tab Counters

**File modified:** `components/admin/admin-damage-claims.tsx`

**Fix:** Same counter pattern as moderation queue:
- Needs Review: Red badge when > 0, "(0)" when empty
- Awaiting Guest: Amber badge when > 0, "(0)" when empty
- Resolved: Always shows count in parentheses

---

#### 11c. Damage Claims RLS Policy Fix

**Why:** Damage claims weren't loading for admins due to missing RLS policy.

**File created:** `supabase/migrations/052_fix_damage_claims_admin_policy.sql`

**Policies added:**
- `property_damage_claims`: Admins can manage all damage claims
- `content_reports`: Admins can view and manage all reports
- `flagged_content`: Admins can manage flagged content

---

#### 11d. Properties Section - Host Profile Picture & Country Column

**File modified:** `components/dashboard/admin-dashboard.tsx`

**Changes:**
- Added host avatar with profile link (clickable to `/profile/[id]`)
- Added "Country" column (displays "England" - prepared for future expansion)
- Updated Supabase query to fetch `id, name, avatar_url` for hosts

---

#### 11e. User Reports Interactive Review

**File modified:** `components/admin/moderation-dashboard.tsx`

**Changes:**
- Added "Review" button with eye icon for each report
- New review dialog shows:
  - Reporter info with profile link
  - Content type and reason badges
  - Reporter's description
  - Content owner info
  - **Actual reported content** (fetches message/review/property/route)
  - Action buttons: Action Taken, Dismiss, False Report
- Content preview shows:
  - Messages: sender → receiver, full message content
  - Reviews: guest name, property name, rating stars, comment
  - Properties: name, location, description
  - Routes: name, creator, description

---

## Codebase Audit Results (Session 1)

| Area | Status |
|------|--------|
| Money handling (pennies) | ✅ Compliant |
| Input sanitization | ✅ Compliant |
| Rate limiting | ✅ Compliant |
| Database migrations | ✅ Compliant |
| RLS policies | ✅ Compliant |
| Fee consistency | ✅ Fixed this session |
| Route naming | ✅ Fixed this session |

---

## Historical Summary

See `changelogs/HISTORICAL_SUMMARY.md` for comprehensive history of all features built before changelog system was established.

---

---

### 12. Moderation Dashboard Overhaul (Session 4 ~13:00 UTC)

**Why:** User requested removal of empty Moderation Queue and improvements to both sections.

**Changes:**

#### 12a. Removed Moderation Queue Tab
- Removed "Content Queue" tab entirely (was always empty)
- Now only two tabs: Flagged Messages and User Reports

#### 12b. Flagged Messages - Added Severity Filter
- New filter dropdown: All Severity, Critical, High, Medium, Low
- Status filter: Pending Only, Reviewed Only, All Messages  
- Shows count of filtered vs total messages

#### 12c. User Reports - Major Overhaul
Now matches Flagged Messages review style:

**Filters Added:**
- Status: Pending Only, Resolved Only, All Reports
- Reason: Filter by report reason (harassment, spam, etc.)
- Content Type: Filter by type (message, review, property, route, comment)
- Shows filtered count

**Review Dialog (matches Flagged Messages):**
- Same layout as Flagged Messages review dialog
- Reporter info with email and trust score
- Content owner info with profile link
- Report reason with colored badge (red for harassment/hate, orange for inappropriate/spam)
- Reporter's description in highlighted box

**Full Context View:**
- For messages: Shows the full conversation between the two users (up to 50 messages)
- Reported message is highlighted with amber background
- Each message shows sender name, timestamp
- For reviews: Shows rating stars, property link, full comment
- For properties: Shows property link, location, description
- For routes: Shows route name, creator, description
- For comments/questions: Shows property link and question text

**Action Selection (same as Flagged Messages):**
- No Action - False Positive
- Warning Sent to User
- Content Removed
- User Suspended
- Mark as False Report

---

### 13. Damage Claims Query Fix (Session 4)

**Why:** Claims failing with 400 error due to FK relationship issues in Supabase query.

**File modified:** `components/admin/admin-damage-claims.tsx`

**Fix:** Changed from single query with FK joins to:
1. First fetch claims with property and booking data
2. Then fetch host and guest info separately via Promise.all
3. Combine data manually

This avoids the complex FK relationship query that was causing the 400 error.

---

### 14. Booking Flow & Fee Display Fixes (Session 5 ~14:00 UTC)

**Why:** User reported "column minimum_nights does not exist" error when booking, and incorrect 15% service fee shown in pricing preview.

#### 14a. Missing Database Column Fix

**File created:** `supabase/migrations/053_add_minimum_nights.sql`

**Fix:** Added `min_nights` column to properties table (with constraint min_nights > 0, default 1).

Note: Codebase uses `min_nights` (not `minimum_nights`). Migration uses `IF NOT EXISTS` since column may exist from earlier migration 026.

---

#### 14b. Corrected Fee Rate References Across Site

**Problem:** Multiple places showed incorrect "15% service fee" when actual rate is 9.5%.

**Files modified:**
- `components/host/advanced-pricing-settings.tsx`
  - Changed calculation from `0.15` (capped at £150) → `0.095` (no cap)
  - Added serviceFeeVat to preview
  - Updated host payout to use 2.5% + VAT calculation
  - Changed UI label from "15%" to "9.5%"
  
- `components/host/steps/pricing-step.tsx`
  - Changed text from "15% / 3%" to "9.5% + VAT / 2.5%"
  
- `app/help/page.tsx`
  - Updated FAQ: "12.5% capped at £150" → "9.5% (no cap)"
  - Updated example calculation
  
- `app/terms/page.tsx`
  - Updated fee display: "12.5% capped at £150" → "9.5% (no cap)"
  
- `components/price-breakdown.tsx`
  - Updated tooltip: "12.5% capped at £150" → "9.5% (no cap)"

---

#### 14c. Booking Form - Fixed `minimum_nights` reference

**File modified:** `components/booking-form.tsx`

**Fix:** Changed `property.minimum_nights` → `property.min_nights` to match actual database column.

**File modified:** `app/property/[id]/page.tsx`

**Fix:** Changed `minimum_nights: property.minimum_nights` → `min_nights: property.min_nights`

---

#### 14d. Host Calendar - Fixed Column References

**Why:** Calendar wasn't loading bookings due to incorrect column names.

**File modified:** `app/api/host/calendar/route.ts`
- Changed `check_in` → `start_date`
- Changed `check_out` → `end_date`

**File modified:** `components/host/booking-calendar.tsx`
- Updated interface: `check_in/check_out` → `start_date/end_date`
- Updated `total_price_pennies` → `total_charge_pennies`
- Fixed all date/price references in component

---

## Fee Structure Summary (AUTHORITATIVE)

| Fee Type | Rate | Cap | VAT |
|----------|------|-----|-----|
| Guest Service Fee | 9.5% | None | +20% VAT |
| Host Platform Fee | 2.5% | None | +20% VAT |

**Example: £200 booking**
- Guest pays: £200 + £19 (9.5%) + £3.80 (VAT) = **£222.80**
- Host receives: £200 - £5 (2.5%) - £1 (VAT) = **£194.00**

---

### 15. Calendar & Discount System Fixes (Session 5 ~14:30 UTC)

**Why:** Calendar not showing "requested" bookings, seasonal discounts not applying to bookings or showing in live preview.

#### 15a. Host Calendar - Show "Requested" Bookings

**Files modified:**
- `app/api/host/calendar/route.ts`
  - Changed status filters: `'confirmed'` → `'accepted' | 'completed'`
  - Changed `'pending'` → `'requested'`
  - Fixed `total_price_pennies` → `total_charge_pennies`
  
- `components/host/booking-calendar.tsx`
  - Added color mapping for `'requested'` and `'accepted'` statuses
  - Added `'completed'` status with blue color
  - Updated legend to show all statuses

**Status color legend:**
- Green: Accepted
- Orange: Requested
- Blue: Completed
- Red: Declined
- Gray: Cancelled

---

#### 15b. Seasonal Discounts in Live Pricing Preview

**File modified:** `components/host/advanced-pricing-settings.tsx`

**Fix:** Added seasonal discount check to the `previewDiscount` calculation:
- Simulates check-in date based on "days before check-in" slider
- Checks if simulated date falls within any active seasonal discount range
- Added `seasonalDiscounts` to the dependency array

---

#### 15c. Discounts Applied to Actual Bookings

**File modified:** `app/api/booking/request/route.ts`

**Changes:**
- Added first-time booking detection (checks user's previous accepted/completed bookings)
- Calls `get_best_discount` database function to find applicable discount
- Applies discount to nightly total (not cleaning fee)
- Stores discount info in booking record:
  - `original_subtotal_pennies`
  - `total_discount_pennies`
  - `discounts_applied` (JSONB with type, name, percent, amount)

---

#### 15d. Discount Display on Booking Form

**File modified:** `components/booking-form.tsx`

**Changes:**
- Added discount state and fetch effect
- Fetches discount when dates change using `get_best_discount` RPC
- Applies discount to calculated price
- Shows discount line in green: "{Discount Name} (X% off) -£Y.YY"

---

*Changelog created by AI Assistant (Claude)*
*Last updated: 2026-01-16 ~14:30 UTC*
